# 063 · 升级/重装免重装机制（node_modules 保留 + 模板补全 + 老 venv 自愈）

> 背景：062 之后，Win 阶段 4 又暴露：**每次升级 / 卸载重装，都要重装 node、cosy 又“缺环境”**；
> 且 sensevoice-original(8002, funasr) 老 venv 起不来。本文给根因与三项修复顺序。
> 关联：001 §6.7（S6′ 版本刷新）、002 S6′（物化）、032 P3（代码目录只读）、062（安装锁/乱码/留底）。
> 状态：①②③④ 代码已落地（2026-09-09），SRC/DEST 同步，版本 **2.10.6**；待重构建后按验收清单实测。

---

## 一、根因（已实测）

### 1.1 升级/重装要重装 node —— 安装产物放在“会被重建”的代码目录里
- 服务代码目录（App 数据目录 `server/`）在版本升级或卸载重装时**整目录重建**；`node_modules` 按设计不打进包（002 S6′）→ 每次重建后 npm ci 重跑一次（node 本体不重下，但 npm ci 要几分钟）。
- 数据根（`E:\Downloads\opensound-download`：models / venvs / voices）是持久的 → whisper / sensevoice(量化) / kokoro 不受影响。

### 1.2 cosy 每次重建后“缺环境” —— 内置模板缺 vendored matcha/models
- `vendor/cosyvoice/third_party/Matcha-TTS/matcha/models`（8 个文件）从 S4 vendoring 起就没进模板；
- engineReadiness（062 补的检查）在**新代码目录**里发现缺 → 报“环境缺失” → 必须「检测/修复」从 jsdelivr 联网补 → 下次整目录重建又没。
- venv（数据根）一直在，问题只出在 vendored 源码缺口。

### 1.3 sensevoice-original(8002) 起不来 —— 老 venv 的 pyvenv.cfg home 指向已删除的别名目录
- funasr 的 `.venv-funasr` 是早期建的：`pyvenv.cfg home = ...\cpython-3.11-windows-x86_64-none`（版本别名）；
- 062 的“删别名链接→复制真实目录回原位”在 python 正运行时复制失败（DLL 占用）→ 别名目录消失、未重建；
- cosy/qwen3 venv 后来被 uv 重建，`home` 写成真实目录 `.16` → 正常；funasr 老 venv 仍指已消失的别名 → uv trampoline `entity not found` → 8002 秒退。
- 结论：**根本不需要复制 base 别名**——uv 能直接发现真实 `.16`；真正要修的是把老 venv 的 `home` 改写为真实目录。

---

## 二、方案（执行顺序）

### ① 启动自愈改进（治 1.3 + 修掉“复制 base”脆弱步骤）—— `src-tauri/src/lib.rs`
1. `normalize_python_base_symlinks`：reparse 别名目录 → **只删除链接，不再复制 base**（uv 能发现真实目录；避免运行中复制 DLL 失败）。
2. 新增“受管 venv home 归一化”：扫描 `<数据根>/venvs/.venv-*/pyvenv.cfg`，凡 `home` 指向“不存在 / reparse / 非补丁别名”路径 → 改写为真实 CPython 目录（`runtime/python/cpython-3.11.*-windows-x86_64-none` 中带完整版本号的真实目录）。
3. 在启动与「安装 Python 基础」时各执行一次（幂等）。
- 验收：8002 老 venv 无需重装即可被拉起；后续版本不再有“复制 base”导致的半成品状态。

### ② 整目录重建保留 node_modules（治 1.1）—— `src-tauri/src/lib.rs` materializer
- 重建时：若旧 `package-lock.json` 与内置新模板的 `package-lock.json` 内容一致，且旧目录有 `node_modules` → 迁移保留（本地改名，秒级）；不一致才丢弃（依赖变了，理应重装）。
- 验收：升 asr-server 版本（依赖未变）→ 不出现“服务端依赖未就绪”，服务直接用。

### ③ matcha/models 打进模板（治 1.2）—— 仓库 `asr-server/vendor` 补全
- 把 8 个文件从 jsdelivr（GitHub 官方同源）vendoring 进仓库 `asr-server/vendor/cosyvoice/third_party/Matcha-TTS/matcha/models/`（DEST 由 stage 自动再生）；
- 062 加的 readiness 检查与 jsdelivr 补拉逻辑保留作兜底（存在即跳过）。
- 验收：整目录重建后 cosyvoice 卡片不再“缺环境”，无需联网修复。

### ④ 版本轮换 2.10.6（触发一次整目录重建以实测 ①②③；node_modules 应被 ② 保留，cosy 不再缺环境）

---

## 三、风险与回退
- ② 的“lock 未变保留 node_modules”只在 package-lock 作为依赖指纹可靠时成立——因此比较对象固定为 `package-lock.json`（含传递依赖锁）；`package.json`/`.npmrc` 变化会反映到 lock（若没反映说明本来就该手动升 lock）。
- ① 只删链接不复制：若未来某机器真实目录也缺失，python_base_status=false → `uv python install` 自动补（原有逻辑）。
- ③ 新增文件为上游 MIT 源码（与现有 vendored 策略一致）。

## 四、验收清单
- [ ] 升版 2.10.6 后不提示装 node（node_modules 保留）
- [ ] 升版后 cosyvoice 就绪、不“缺环境”
- [ ] 8002 sensevoice-original 正常拉起出声
- [ ] 卸载重装/升级不再“每个模型环境重装”

## 五、变更记录
| 日期 | 变更 |
| --- | --- |
| 2026-09-09 | 初稿：登记 1.1–1.3 三个根因；用户拍板三项全做（顺序 ①→②→③→④） |
| 2026-09-09 | 落地：① lib.rs——不再复制 base（只删 reparse 别名），新增 real_python_base + normalize_venv_homes（老 venv home 自动改写真实目录），启动/「安装 Python 基础」均执行；② 物化重建时 package-lock 一致则暂移并放回 node_modules；③ matcha/models 8 文件 vendoring 进 SRC/DEST 模板；④ 版本升 2.10.6；cargo check + node --check + diff 校验通过 |
