# 000-plan-15 · 接下来工作指南（跨平台遵循 + 文档纪律）——开发用途

> 定位：**开发者内部文件**，供 Win / mac 两边协作遵循与后续工作排程参考。用户自行 commit。
> 适用范围：所有改动人员；属于「接下来怎么干活」的约定，不是产品/用户文档。
> 状态：2026-09-09 起草。

---

## 一、当前状态（一句话）

阶段 4（Win 复验 S6′ 单产物自包含）**功能面已全绿**：node 免重装、cosy（matcha 模板）、8002 老 venv 重建、安装锁/编码/留底均已修复并本机验证（062/063 收尾）。macOS 从零验收由本人在 mac 侧执行。

## 二、文档纪律（用户拍板 2026-09-09）

1. **0 开头编号文档（`00x-*` / `000-*` / `000-plan-*`）一律为开发用途**；不出现在任何用户可见渠道。
2. **README / 官网 / 发布说明 / App 内文案不得引用这些开发文档编号**（README 已清理为纯用户向内容，只留 SPEC/GUIDE）。
3. `000-plan-*` 仅开发者查阅，不对外宣传；内容允许直接引用其它 0xx 开发文档。
4. 新增“面向用户”的文字一律按「能做什么 / 怎么装 / 数据在哪」写，不写实现细节与文档编号。
5. 本仓库部分开发文档只在本地（未传 mac/远端）属正常，不因缺失补传；引用缺失文档（如 000-plan-14）不代表必须补写——确认宿主确实需要再写。

## 三、跨平台改动纪律（「跨平台遵循」的核心）

任何改动若触及以下任一领域，必须**两平台都核对并能在另一平台回归后再算完成**：

- 进程 spawn / 生命周期 / 信号（Unix SIGTERM vs Win taskkill 树杀）
- 路径形态（`bin/python3` ↔ `Scripts/python.exe`；App 数据目录 vs 数据根）
- **编码**（Win 子进程默认 GBK；新 spawn 一律注入 `PYTHONUTF8`；读管道按行 utf-8→gbk 回退）
- **文件系统差异**：`fs::canonicalize` 在 Win 返回 `\\?\` verbatim（勿直接喂给 node）；reparse 链接/junction 的权限信任（os error 44）
- venv / uv 受管 CPython 语义（老 venv 的旧 trampoline 只能重建）
- 物化 / 版本指纹（asr-server 逻辑改动必须同步 `SERVER_VERSION`×SRC/DEST + start-all `EXPECTED_VERSION` + `.version`，升版后按 063 验证“node_modules 保留”不回归）

**速查表**：Win/mac 已知差异八条见 `001-跨平台适配 §6.9`；逐坑记录见 `000-summary §7.9`；mac 回归对照也用它。

## 四、接下来工作清单

### A. 收尾确认（Win 本机）
- [x] 062 验收（连续安装零 409 / 乱码 / 日志留底）——2.10.5 起逐项实测
- [x] 063 验收（升版不重装 node / cosy 就绪 / 8002 可用）——2.10.6→2.10.8
- [ ] **D3：干净第二台 Win PC 从零验收**（安装→引导条→逐引擎→出声出字→重启保持→卸载/清理）

### B. mac 侧（本人执行，完成后回填 001/000-summary/README 用户向口径）
- [ ] mac 从零验收（001 §五·补 10 项 + §6.4 P2 清单）
- [ ] P1 代码补齐：`ensure_node` darwin-x64（Intel）、平台分支清点、mac 走 CPU/MPS wheel 确认
- [ ] MPS 系引擎（qwen3/cosy 9GB）在 Win 改动后真机回归（001 §6.9 表当对照）

### C. 发布前置（拍板后另行排期）
- Windows 签名 / macOS 公证与 Gatekeeper 引导
- 无 WebView2 老系统的离线兜底口径
- README/发布说明的用户向定稿（不含开发文档引用）

### D. 后续立项候选（另有 000-plan-16/17，勿与本表重复排）
- 000-plan-16：运营推广（框架待本人细化）
- 000-plan-17：indextts / firered 等“独立部署先行→回 App”引擎集成

## 五、变更记录
| 日期 | 变更 |
| --- | --- |
| 2026-09-09 | 起草：文档纪律（0xx=开发用途、不进用户渠道）、跨平台改动纪律（引 001 §6.9 / 000-summary §7.9）、阶段 4 收尾状态与后续清单 |
