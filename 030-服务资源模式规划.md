# 030 · 本地服务资源模式规划（节能 / 全能 / 按需加载）

> 回答一个问题：**"这台电脑同时常驻多少个本地模型服务合适？"**——本文把 OpenSound 的服务启动策略从"全拉起"演进为"按需占资源"，并给出当前（节能开关）、中期（空闲卸载）、长期（统一按需加载器）三阶段落地路径。
> 口径：2026-08-26 · 内存/加载时间数字复用本仓库已实测登记口径（006 纪律：不得猜测，未实测标 ⚠️）。
> 关联文档：`002-plan`（服务自举与分发）、`006`（模型体积实测登记）、`000`（设备画像→模型过滤）、`start-all.js`（现状全拉起）。

---

## 一、背景与动机

- 全能模式（现状，`start-all.js` 全拉起）在 32GB+ 机器无压力，但在 **16GB 机器上会触发 swap 卡顿**（全开约 12–16GB）。
- 用户诉求：需要"省资源"的选项，接受"切模型时等一等"；也确认了"随时可用"的价值。
- 现有手动节能能力（`OPENSOUND_SKIP_*` 环境变量）**未暴露到 UI**，普通用户不可见不可用。

---

## 二、现状盘点（实测登记口径）

| 服务 | 端口 | 常驻内存 | 冷启动等待 | 现状控制开关 |
| --- | --- | --- | --- | --- |
| asr-server（sherpa ASR + kokoro TTS + LLM 运行时） | 9528 | ~1GB（不含 LLM） | 秒级 | — |
| qwen3-tts（TTS 高音质） | 8001 | 2–4GB | 数十秒 | `OPENSOUND_SKIP_QWEN3=1` |
| sensevoice-original（funasr 高精度 ASR） | 8002 | 1.5–2.5GB | 20–60s | `OPENSOUND_SKIP_SENSEVOICE_ORIGINAL=1` |
| cosyvoice-tts（克隆） | 8003 | **4–6GB** | **1–2 分钟** | `OPENSOUND_SKIP_COSYVOICE=1` |
| LLM Qwen3-8B（llama-cpp，9528 进程内） | — | 6–8GB | 5–10s（mmap） | 对话面板引擎切换（卸载即时） |

> 结论：大头的内存 = cosyvoice（4–6GB）+ LLM 8B（6–8GB）；ASR 与 kokoro 是轻量常驻，无节能必要。
> 等待代价不对称：LLM 5–10s 可接受；**cosyvoice 1–2 分钟是节能模式的主要代价**。

---

## 三、三种模式定义

| 模式 | 行为 | 代价/收益 | 状态 |
| --- | --- | --- | --- |
| **全能模式**（现状） | 全部服务常驻，随时切换 | 占 12–16GB；零等待 | ✅ 已实现（start-all 全拉起） |
| **节能模式**（v1） | 轻量常驻（9528 本体）；大模型（qwen3/cosyvoice/funasr/8B）按用户开关不预启动，用时手动/自动拉起 | 省 10GB+；切大模型等冷启动 | 🔶 本文阶段一 |
| **按需加载模式**（v2/v3） | 服务进程常驻但模型空闲卸载（Ollama keep_alive 式）；调用自动重载 | 省内存且等待只发生在冷启动 | 🔶 本文阶段二/三 |

---

## 四、阶段规划

### 阶段一（当前）：节能模式 v1 —— UI 开关 + 重启生效 ✅ 已实现

**目标**：普通用户能在设置页一键选择"节能 / 全能"，无需懂环境变量。

**怎么做**：
1. Rust `UiSettings` 增加字段：`skip_qwen3 / skip_cosyvoice / skip_sensevoice_original`（bool，默认 false = 全能），随 `get/set_ui_settings` 读写（config.json ui 节，011 §5.6 存储规范）。
2. `start_service` 读 config 后注入对应 `OPENSOUND_SKIP_*` 环境变量到 `start-all.js`（`...process.env` 已透传）。
3. UI `SettingsPanel` 新增「服务资源模式」区：
   - 三个大模型服务开关（Qwen3 TTS / CosyVoice 克隆 / SenseVoice 原始版），每个标注常驻内存与冷启动等待；
   - 一键「节能模式 = 全部关闭 / 全能模式 = 全部开启」；
   - 保存后调用 `start_service_cmd`（重启服务生效），并提示"切换模型需等待冷启动"。
4. LLM 档位：沿用对话面板/工作台引擎切换（llama-cpp 按请求加载，同刻只加载一个 GGUF，切换即时）。节能默认已装最小档位，用户自选持久化（000-plan-3）。

**落地记录（2026-08-26 实现）**：字段改为 `power_mode: "full"|"eco"` + `eco_big: "none"|"qwen3"|"cosyvoice"|"sensevoice-original"`（节能下大模型单开，其余关闭）；`start_service` 按组合注入 `OPENSOUND_SKIP_QWEN3/COSYVOICE/SENSEVOICE_ORIGINAL`；设置页「服务资源模式」区含模式单选 + 节能大模型单选 + 「应用并重启服务」+ 克隆音色依赖提示。最小集 = 9528 本体（sherpa SenseVoice + Kokoro + llm-0.5b），无需任何额外服务，天然保证可用。持久化：config.json ui 节，重启沿用用户选择。

**设计更新（2026-08-31 决策：节能模式 8B 彻底禁用）** ⚠️ **2026-09-04 被 000-plan-3 推翻，见下方更正**：
- 背景：原设计 8B 是对话面板"按请求加载"（节能下选了会切 eco_big=8b 自动启用，占 6–8GB 内存，与"省资源"意图相悖）。
- 变更：**节能模式下 8B 不可用**——`eco_big` 移除 `llm-qwen3-8b` 选项（设置页/类型/后端分支同步删）；`start_service` 节能时注入 `OPENSOUND_SKIP_LLM_8B=1`，asr-server 对话接口拒绝 8B 加载（后端双保险，防前端绕过）；对话面板 8B 选项节能下置灰（「节能模式禁用」）；模型管理页 8B 卡片节能下显示「可用 · 未启用（节能模式）」。
- 结果：节能 = 9528 最小集（SenseVoice 量化 + Kokoro + Whisper + llm-0.5b）+ `eco_big` 单开一个 Python 大模型（qwen3 / cosyvoice / sensevoice-原始）；**8B 仅全能模式可用**。
- 旧 config 残留 `eco_big="llm-qwen3-8b"` 自动落 `("eco", _)` 默认分支（Python 全关 + 8B 禁用），无需迁移。

**2026-09-04 更正（用户拍板，000-plan-3 §一为权威）——节能模式的正确意图**：
- **节能 = 每个类别同时只启用 1 个模型**（避免同类别多模型同时占用内存），**不是"禁用某模型"**。
- LLM 类别（0.5B/8B/未来档位）在 9528 进程内同刻只加载一个 GGUF，天然满足"类别单开"——**8B 不是被禁**：用户选 8B 就加载 8B（占 6–8GB 由用户自决）。
- 节能默认：每类启用**已安装中主文件最小**的模型（LLM：0.5B 469MB < 8B 5.0GB）；用户自选 → **持久化**（config `ui.llm_model`）并一直按用户选择执行。
- 已按此修正的代码：删除 `OPENSOUND_SKIP_LLM_8B` 注入（Rust）与后端 8B 拦截（asr-server）、对话面板 8B 置灰/强制回落、模型页 8B「未启用」徽标；回落逻辑改为「当前档位未装 → 已保存选择（若装）→ 节能=已装最小 / 全能=8B 已装则 8B」；`llm_model` 选择持久化（config ui 节）。
- `eco_big` 仍管 **Python 大模型**（qwen3 / cosyvoice / sensevoice-原始 属于 TTS/ASR 类别的大引擎，全局单开其一）——与 LLM 档位互不影响。

**验收标准**：
- 设置页关闭 cosyvoice 后重启服务 → 8003 不再监听，模型管理页 CosyVoice 徽标显示"未启用（节能）"而非 🚫/缺文件；
- 全能模式恢复全部拉起；两个模式往返无残留进程。

**涉及文件**：`src-tauri/src/lib.rs`、`ui/src/panels/SettingsPanel.tsx`、`ui/src/types.ts`（设置类型）、`start-all.js`（仅确认透传）。

### 阶段二（中期）：按需加载 v1 —— 模型空闲卸载（进程常驻）

**目标**：服务进程常驻（端口在、状态可见），但**大模型空闲 N 分钟自动卸载**，内存回落；再次调用自动重载并提示等待。

**怎么做**：
1. Python 侧（`cosyvoice-tts-server.py`、`sensevoice-server.py`）：模型引用改为"最近使用时间戳 + 空闲定时器"，`idle_after=300s` 无请求 → `del` 模型 + `gc.collect()`（torch 需释放缓存）；`/health` 返回 `{ loaded: false }`，请求时若未加载先重载（日志 + 响应延迟）。
2. LLM（asr-server.js llama-cpp）：复用 `llmInvalidate()` 思路，加空闲卸载定时器（如 10 分钟无对话释放 session/模型），首次对话多等 5–10s。
3. UI：模型管理页为每个大模型显示三态「运行中 / 已卸载（空闲回收）/ 加载中」；点击或调用自动加载。
4. 配置：`idle_after` 可在设置页调整（默认 5 分钟）。

**验收标准**：
- 16GB 机器：cosyvoice 加载后空闲 5 分钟 → 内存回落 4–6GB，`/health loaded=false`；再克隆一次自动重载成功（等待 ≤ 1–2 分钟，UI 显示"加载中"）。
- 反复加载/卸载无内存泄漏（观测 RSS 不回涨）。

**涉及文件**：`asr-server/cosyvoice-tts-server.py`、`asr-server/sensevoice-server.py`、`asr-server/asr-server.js`、`ui/src/panels/ModelsPanel.tsx`、`ui/src/panels/VoicePanel.tsx`、`ui/src/panels/AsrPanel.tsx`。

### 阶段三（长期）：统一按需加载器（Ollama keep_alive 式）

**目标**：把"按需加载"做成通用机制，asr-server 成为 mini-Ollama——引擎注册表 + 统一加载/卸载/保活策略 + 智能预加载。

**怎么做**：
1. asr-server 内建 `MODEL_RUNTIME` 注册表：`{ engine → { load(), unload(), keepAlive, idleSince, status } }`，统一管理 Python 子服务与 llama-cpp；
2. 统一策略：`keep_alive`（默认 5 分钟空闲卸载）+ 手动 pin（常驻）+ 内存水位抢占（内存紧张时先卸载未 pin 的大模型）；
3. 智能预加载：按最近使用频率/当前面板预加载（如用户停留在"音色克隆"面板则保活 cosyvoice）；
4. 端到端度量：启动时把各服务实测内存/加载时间回填 `006` 与 `000` 资源表（007 纪律）。

**验收标准**：
- 任意 16GB 机器安装后"打开即用"且不卡：默认全能体验（常用模型自动保活）+ 内存水位保护；
- 无任何需要用户手动理解"模式"概念的开关（开关仅作为高级选项保留）。

**涉及文件**：`asr-server/asr-server.js`（注册表与调度）、`asr-server/start-all.js`（懒启动改造）、`ui`（状态展示）、文档 `006/000`（登记回填）。

---

## 五、数据来源与纪律

- 常驻内存/加载时间：本文表格为 2026-08-26 估算口径，**阶段一落地时逐服务实测并回填 006 变更记录**（007 纪律：禁止写死猜测值）。
- 冷启动等待随硬件变化：以 `000` 设备画像档位为参照标注。
- 所有开关默认值 = 全能模式（不改变现有用户体验）。

## 六、变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-26 | 初版：三种模式定义 + 三阶段规划（节能开关 → 空闲卸载 → 统一按需加载器） |
| 2026-08-26 | 阶段一落地：`power_mode`/`eco_big` 持久化 + `start_service` 注入 `OPENSOUND_SKIP_*` + 设置页「服务资源模式」区（节能/全能 + 节能大模型单开 + 应用重启 + 克隆依赖提示） |
| 2026-08-31 | 设计更新：**节能模式 8B 彻底禁用**——`eco_big` 移除 `llm-qwen3-8b`（设置页/类型/后端分支同步删）；`start_service` 节能注入 `OPENSOUND_SKIP_LLM_8B=1`；asr-server 对话接口拒绝 8B（双保险）；对话面板 8B 置灰、模型页 8B 显示「未启用（节能）」；8B 仅全能模式可用 |
| 2026-09-04 | **推翻"8B 禁用"（用户拍板，000-plan-3 §一）**：节能正确意图 = **每类同时仅启用 1 个模型**（无禁用）；LLM 同刻只加载一个 GGUF，8B 是普通可选项。代码修正：删 Rust `OPENSOUND_SKIP_LLM_8B` 注入与 asr-server 8B 拦截、对话面板 8B 置灰/eco 强制回落、模型页 8B「未启用」徽标；回落 =「已保存选择（若装）→ 节能=已装最小（按主文件 diskGB）/ 全能=8B 已装则 8B」；`config ui.llm_model` 持久化用户 LLM 档位选择。详见 000-plan-3 |
| 2026-09-04 | **第二批实施（000-plan-3 §四）：每类启用选择取代全局 eco_big**——config 加 `eco_tts`/`eco_asr`（TTS/ASR 各类同时仅启用 1 个引擎，含轻量：Python 真停进程、轻量前端停用）；旧 `eco_big` 仅兼容回退（Rust effective_eco + 前端双回退）；Rust start_service 按类注入 skip；新增 `EcoResourceTables.tsx`（模型页顶部按类别资源表：模型/主文件/磁盘/内存/当前启用 + 点选启用 + 应用默认(最小已装)）；设置页/朗读/识别/对话/工作台/模型卡全按每类语义联动（下拉停用标注 + 点选切换重启 + 切节能遗留引擎自动回落）；`/models` 输出 profile+mainFiles。详见 000-plan-3 |
