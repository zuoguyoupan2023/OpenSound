# 000-plan-3 · 节能模式语义修正与 LLM 档位管理规划（2026-09-04）

> 定位：**节能模式（030 §四阶段一）语义的官方澄清与修正** + LLM 档位"默认/回落/持久化"规划。
> 起因：对话面板"只装 0.5B 却报 8B 缺失"排查（000-plan-2 §十）暴露出**节能模式 = "禁用 8B"是错误表述**，与用户真实意图不符；且上会话声称的"回落修复"未进代码（已在 000-plan-2 变更记录更正）。
> 用户 2026-09-04 拍板（本文件依据，最高优先级）：节能模式的**正确意图**如下 §一，此前任何文档/代码与此冲突处一律以本文件为准并修正。

---

## 一、节能模式的正确意图（用户原话整理）

1. **节能不是"禁用某个或某几个模型"**，而是：**每个类别同时只启用 1 个模型**，避免同类别多个模型同时启用占用内存。
   - LLM 类别（llm-0.5b / llm-qwen3-8b / 未来新增）天然满足：llama-cpp 同一时刻只加载一个 GGUF（`llmSession` 换模型即重载）。
   - 因此 **8B 不是被禁**：用户选 8B 就启用 8B（关掉同类别先前加载的模型），只是占用 6–8GB 由用户自己决定。
2. **切换语义**：关闭同类别旧模型，启用用户选中的新模型。
3. **默认选择**：节能模式下，每个类别默认启用**该类别已安装的主文件最小**的模型。
4. **用户选择优先并持久化**：用户自己做过选择 → 保存选择并一直按用户选择执行（不再默认/回落覆盖）。
5. **每类三个动态表（第二批规划）**：每个大类自动维护模型表——模型主文件、占用内存（memNeedGB）、已安装状态；当前支持的模型全部列上；排序/默认依据「已安装 − 内存大小」逻辑。

---

## 二、现状与差距（"8B 禁用"错误表述的全部落点，第一批逐一修正）

| 位置 | 现状（错误/过时） | 修正为 |
|---|---|---|
| 000-plan-3 §一意图 | — | 节能 = 每类同时仅启用 1 个；无禁用；默认最小；用户选择持久化 |
| `asr-server/asr-server.js` | `SKIP_LLM_8B` 读 env，对话接口拦截 8B（930-933 行） | 删除拦截与 env 读取（节能下 8B 可加载） |
| `src-tauri/src/lib.rs` | start_service 节能注入 `OPENSOUND_SKIP_LLM_8B=1`（1604-1621/1652 行） | 删除注入与注释 |
| `ui/src/panels/ChatPanel.tsx` | 8B 置灰「节能模式禁用」+ pickLlmModel 拦截 + eco effect 强制 8B→0.5B | 8B 为普通可选项；回落 = 已装最小（无禁用） |
| `ui/src/panels/ModelsPanel.tsx` | 8B 塞进 `BIG_ENGINE_KEY` 大引擎表显示「未启用（节能）」 | 8B 回归普通 LLM 卡（LLM 无独立进程，不存在"未启用"态） |
| `ui/src/api.ts` | `ecoDisabled` 含 llm-qwen3-8b（579 行）；`EcoBig` 注释"8B 不再作为可选项" | ecoDisabled 去掉 8B；注释更新 |
| `030-服务资源模式规划.md` | 2026-08-31 决策"节能模式 8B 彻底禁用"（§四阶段一 / §六变更记录） | 改写为每类单开 + 默认最小 + 用户选择持久化；变更记录补更正行 |
| `000-summary.md` | §九"8B 节能禁用（OPENSOUND_SKIP_LLM_8B 后端双保险…）" | 改写正确意图；§三 LLM 行无禁用表述 |

**可复用资产**：`engines/*.json` 每个模型已含 `profile { diskGB, memNeedGB, accel, tierRequired }` + `checks`（主文件路径 + `bytes`）→ "最小模型"与"三表"数据源齐备；但 `/models` 目前**未输出 profile**，第一批补齐（回落排序 + 三表都需要）。

---

## 三、第一批（本次执行）：LLM 档位语义修正 + 回落/默认/持久化

### 目标行为

- **初始档位**：`config.json ui.llm_model`（用户上次选择）→ 未保存时：全能默认 `llm-qwen3-8b`（已装才生效）；节能默认**已装中主文件最小**。
- **回落**：当前选中档位未安装 → 自动切到「已保存选择（若已装）→ 已装中最小」；一个都没装 → 不回落，保留"未下载"引导（后端报缺提示）。
- **持久化**：用户在对话面板/工作台切换 LLM 档位 → `updateSettings({ llmModel })` 存 config.json，重开沿用。
- **移除"8B 禁用"链**：后端拦截、Rust 注入、前端置灰、模型页徽标、文档表述全部按 §二 表修正。

### 改动清单（文件 × 改动）

1. `asr-server/asr-server.js`：删 `SKIP_LLM_8B` 读取与 `llmChat` 拦截；`/models` 每项输出 `profile`（mf.profile：diskGB/memNeedGB 等）。
2. `src-tauri/src/lib.rs`：start_service 删 `skip_llm_8b`（含 1606 元组、1620、1652 行与注释）；`UiSettings` 加 `llm_model: String`（serde default）；`get_ui_settings`/`set_ui_settings` 读写该字段。
3. `ui/src/types.ts`：`ModelInfo` 加 `profile?: { diskGB?: number; memNeedGB?: number; … }`。
4. `ui/src/api.ts`：`PersistedSettings` 加 `llmModel?`；`initSettings` 读 `ui.llm_model`；`updateSettings` 透传 `llmModel`（invoke 参数）；`ecoDisabled` 去掉 `llm-qwen3-8b`；`EcoBig` 注释更新。
5. `ui/src/panels/ChatPanel.tsx`：删 eco8bDisabled/置灰/旧 eco effect/pickLlmModel 拦截；初始 `llmModel` 取已保存值；新增"当前未装→回落已装最小（优先保存值）"effect；档位变化即保存（防抖）；下拉列出已装（无禁用项）。
6. `ui/src/panels/HomePanel.tsx`：同上（工作台同款逻辑）。
7. `ui/src/panels/ModelsPanel.tsx`：`BIG_ENGINE_KEY` 去掉 `llm-qwen3-8b`（及相关 eco 徽标分支对 LLM 不再适用）。
8. 文档：本文件 + 030 §四阶段一与变更记录 + 000-summary §九 + 000-plan-2 变更记录尾。

### 第一批验收

- tsc 通过；用户重建前端后：
  - 全能 + 仅装 0.5B：对话面板/工作台自动选中 0.5B，直接对话成功（无 8B 缺失报错）。
  - 节能 + 装 0.5B+8B：默认选中 0.5B；下拉选 8B 不被拦（LLM 换载 8B，后端无"节能禁用"文案）。
  - 用户切到 8B → 重开 App → 仍显示 8B（选择已保存）；8B 卸载后重开 → 回落 0.5B。
  - 模型页 8B 卡：不再显示"未启用（节能模式）"，文件就绪即绿。

---

## 四、第二批（已实施 2026-09-04，用户拍板三项后落地）：模型页「每类别资源表」+ 每类启用选择

### 落点与形态（用户拍板）
- **位置**：模型管理页**顶部新增区块**（设备摘要条下）——新组件 `ui/src/panels/EcoResourceTables.tsx`。
- **切换范围**：**全部模型都可停/启（含轻量）**——粒度（用户拍板）：Python 大模型（8001/8002/8003）真停进程（Rust 按类注入 `OPENSOUND_SKIP_*`）；9528 内轻量引擎（量化 sensevoice/kokoro/whisper）前端停用（面板下拉停用/点选切换，后端 API 不计入节能语义）；9528 开放 API 仍可直呼。
- **表结构**：每大类一张动态表（LLM/ASR/TTS 三张），行=模型，列 = **模型 | 主文件 | 磁盘大小 | 内存需求 | 当前启用**。数据 = `/models`（新增 `profile`{diskGB/memNeedGB} 与 `mainFiles` 字段输出，源 engines/*.json）。

### 配置模型（取代全局 eco_big）
- eco 配置 = 每类启用选择：`config ui.ecoTts`（TTS：kokoro/qwen3/cosyvoice-clone）+ `ui.ecoAsr`（ASR：sensevoice/whisper/sensevoice-original）+ 已有 `ui.llm_model`（LLM 档位）。
- `eco_big` 字段保留仅兼容旧 config：Rust `effective_eco()` 与前端 initSettings 双重回退映射（qwen3→ecoTts、cosyvoice→ecoTts=cosyvoice-clone、sensevoice-original→ecoAsr），新 UI 不再写它。
- Rust `start_service` 按类注入：skip_qwen3 = eco && ecoTts≠qwen3；skip_cosy = eco && ecoTts≠cosyvoice-clone；skip_sense_orig = eco && ecoAsr≠sensevoice-original。
- 进入节能（HomePanel 快捷 / 设置页「应用并重启」）：未配置的类别自动补「已装中主文件最小」（`applyEcoDefaults`，仅补空位、不覆盖已有选择）。

### 节能默认最小与用户选择
- 默认（无选择）= 每类**已装中主文件最小**（diskGB，`suggestMinEngine`）。
- 用户选择 → 持久化 config 并一直按选择执行（LLM 即时换载不重启；TTS/ASR 切换 = `switchEcoEngine` 重启服务）。
- 资源表提供「应用默认（每类最小已装）」一键回系统默认。

### 面板联动（前端停用表达）
- 朗读（ReadPanel/ChatPanel/HomePanel）与识别（AsrPanel/HomePanel）下拉：节能下非启用项标「（点选切换并启用）」，点选 = 确认 → `switchEcoEngine` → 重启；当前引擎不在启用集（切节能前的遗留）→ 自动回落该类别启用引擎。
- 模型页卡片：进程型大引擎（qwen3/cosyvoice-clone/sensevoice-original）节能未启用 → 「可用 · 未启用（节能模式）」+ 引导到资源表；9528 内轻量显示真实运行态（节能停用由面板/资源表表达）。
- 设置页「服务资源模式」区重做：节能下按 TTS/ASR/LLM 三类别分别单选启用引擎（未下载项置灰）；LLM 说明指向对话面板档位。

### 待补（登记）
- 后端 `/transcribe`、`/voice-chat` 的 `engine=auto` 仍只在 in-process 两引擎里挑（不覆盖 sensevoice-original）；节能下若类启用原始版，面板已引导去「识别」面板显式选，auto 语义未扩（后续如需可加）。
- `switchEcoBig` 标记 @deprecated 未删（无引用后可清）。

---

## 五、Mac 方向调研：Apple Silicon 加速（MLX / MPS / CoreML）与模型版本

> 2026-09-05 桌面调研（公开资料快照）。**本机为 Win，以下全部为 Mac 规划输入，凡未在本机/真机实测者一律标注 ⚠️ 待验证**。执行基准仍是 000-plan-2 阶段 E（mac 从零）与 001（跨平台差异清单）。

### 5.1 加速层事实（Apple Silicon）

| 加速层 | 是什么 | 覆盖引擎（本项目相关） | 备注 |
|---|---|---|---|
| **MPS**（Metal Performance Shaders，torch 内建） | torch 在 Apple Silicon 的后端，macOS 12.3+ | qwen3-tts / cosyvoice / sensevoice-原始（torch 系） | 本项目 mac 现设计口径（030/000：mac 严禁 CUDA torch，走 MPS）。⚠️ MPS 算子覆盖不如 CUDA，部分模型/算子会回落 CPU；cosyvoice 在 MPS 的完整兼容性未实测 |
| **MLX**（Apple 官方框架，仅 Apple Silicon） | 为 LLM/语音专门优化的数组框架；通常显著快于 MPS naive 路径 | **Kokoro / Qwen3-TTS / CSM 得到 mlx-audio 官方支持**（来源：TTS benchmarks 讨论）；Whisper 有 mlx-whisper / whispermlx；Qwen3-TTS 有多个社区 MLX 实现 | 社区实测 Qwen3-TTS 在 Apple Silicon 有 ~10 倍级提速案例 ⚠️ 数字未在本项目复现 |
| **CoreML**（onnxruntime-coreml 执行提供者） | onnx 模型转 .mlpackage 后走神经网络引擎 | sherpa-onnx 系（sensevoice 量化 / whisper onnx） | ⚠️ 未验证；onnx→mlpackage 转换与精度差异需实测 |
| CPU | — | Intel Mac 唯一路径 | Intel Mac 无 MPS/MLX → 本项目在 Intel Mac = 纯 CPU 轻量档（识别/朗读可用，体验打折） |

### 5.2 模型版本事实（关键结论：**不需要"Mac 专用模型文件"**）

- Qwen3-TTS / Kokoro / Whisper / LLM（GGUF）：MLX/MPS/Metal 都是**同一份权重、换推理框架**跑，不是下载不同模型文件。本项目下载到 `models/` 的快照（qwen3 hf hub / kokoro onnx / whisper / GGUF）**mac 直接复用，零额外模型下载**。
- 例外/注意：onnx→CoreML 需要转换产物（.mlpackage，可下载或转换）；node-llama-cpp（LLM）走自带预编译 Metal 二进制（**需 macOS 15+**，macOS 14 会静默失败——来源：node-llama-cpp issue #558，⚠️ 本机未实测）。

### 5.3 与现设计的关系与接入建议（规划输入，未实施）

1. **LLM（llama.cpp）**：现 node-llama-cpp 跨平台同依赖，mac 用 Metal 预编译（系统版本门槛 5.2 登记）；无需新引擎。
2. **Kokoro / Qwen3-TTS → mlx-audio**：若阶段 E 实测 MPS 路径慢，候选 = 在 mac 安装器装 `mlx-audio` 并以 mlx 推理替代/并行 8001（新引擎类型或运行时选择），权重复用。
3. **Whisper / sensevoice 量化 → onnxruntime-coreml**：sherpa-onnx mac 走 CoreML 提供者加速候选 ⚠️ 需先测 onnx 全链兼容。
4. **Torch 三引擎的 CPU/GPU 双版本**（本批 Win 逻辑）：mac 无"CPU/GPU 双按钮"（无 NVIDIA）；等价物 = 无按钮只 CPU（MPS 由 torch 自动用，安装器 mac 分支早已禁 CUDA）。
5. **系统版本矩阵**：MPS 12.3+ / node-llama-cpp Metal 15+ / MLX 门槛以官方为准（实施时核）——阶段 E 需登记目标机型系统版本。
6. **范围纪律**：以上优化**只影响 mac 体验**，Win 侧无改动；列入阶段 E（mac 从零）前置调研项，不在 Win 主线执行。

### 5.4 参考来源（2026-09-05 快照，均为外部公开资料）

- Qwen3-TTS MLX 社区实现：[odiak/Qwen3-TTS-MLX](https://github.com/odiak/Qwen3-TTS-MLX)、[kapi2800/qwen3-tts-apple-silicon](https://github.com/kapi2800/qwen3-tts-apple-silicon)（M1-M4 离线 + MLX 克隆）
- Apple Silicon 提速案例：[Qwen3-TTS を Apple Silicon で 14.8 倍高速化（MLX）](https://note.com/anotete_cto/n/nc332da37912f)、[MLX-Audio 推理优化实践](https://blog.hotdry.top/posts/2026/01/24/mlx-audio-apple-silicon-inference/)
- mlx-audio 引擎支持与 benchmark：[TTS Performance Benchmarks on Apple Silicon（MLX-Audio officially supports Kokoro, Qwen3-TTS, CSM）](https://github.com/ragaeeb/kakoo/issues/23)
- Whisper MLX：[whispermlx (PyPI)](https://pypi.org/project/whispermlx/)、[KalebJS/whispermlx](https://github.com/kalebjs/whispermlx)
- Mac 上跑 Qwen3-TTS/CosyVoice2·3 的社区记录：[zenn（Apache2 voice clone 在 Mac）](https://zenn.dev/mohhh_ok/articles/2026-06-cosyvoice-qwen3-tts-apache2-mac)
- node-llama-cpp Metal 系统版本门槛：[issue #558（Prebuilt Metal 需 macOS 15+，14 静默失败）](https://github.com/withcatai/node-llama-cpp/issues/558)

---

## 六、变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-04 | 初版：节能语义澄清（每类单开/默认最小/用户选择持久化/无禁用）+ 第一批修正清单（移除"8B 禁用"链 + 回落按已装最小 + llm_model 持久化）+ 第二批三表规划 |
| 2026-09-04 | **第一批已实施（待用户重建验收）**：① asr-server.js 删 SKIP_LLM_8B 拦截 + `/models` 输出 profile；② lib.rs 删 OPENSOUND_SKIP_LLM_8B 注入 + UiSettings 加 `llm_model` + set_ui_settings 透传；③ api.ts/types.ts：PersistedSettings.llmModel / ModelInfo.profile / ecoDisabled 去 8B；④ ChatPanel+HomePanel：初始档位=已保存选择，回落=「已保存（若装）→ 节能=已装最小(profile.diskGB) / 全能=8B 已装则 8B」，选择即存 config；⑤ ModelsPanel：8B 移出 BIG_ENGINE_KEY（恢复普通 LLM 卡）；⑥ 文档更正（030/000-summary/000-plan-2 + 本文件）。静态检查全过（tsc / node --check / cargo check）。 |
| 2026-09-04 | **第二批已实施（待用户重建验收，三项拍板：模型页顶部 / 全模型可停启 / 五列含主文件）**：① `/models` 加 `mainFiles` 输出；② Rust：UiSettings 加 `eco_tts`/`eco_asr` + `effective_eco()`（eco_big 回退）+ start_service 按类注入 skip；③ api.ts：`EcoTts/EcoAsr` 类型、`ecoActiveEngine/engineDisabledInEco/suggestMinEngine/applyEcoDefaults/switchEcoEngine`、computeStarting/ecoDisabled 重构、initSettings eco_big 回退；④ 新组件 EcoResourceTables.tsx（模型页顶部三表 + 点选启用 + 应用默认）+ App.css 样式；⑤ 面板联动（ReadPanel/ChatPanel/HomePanel/AsrPanel/VoicePanel/SettingsPanel/ModelsPanel 全改每类启用语义）；⑥ 文档同步。静态检查全过（tsc/node/cargo）。
| 2026-09-04 | **第二批用户验收通过**；清理：删除废弃 `switchEcoBig`（无引用，api.ts）；000-plan-2 §十快照刷新（已完成 6 项 + 阶段 D 逐引擎下载验证待办）；§四「待补」余项：`/voice-chat` 的 `auto` 覆盖 sensevoice-原始（可选，未做） |
| 2026-09-05 | **§五 新增 Mac 方向调研（桌面快照）**：MLX/MPS/CoreML 分层事实；关键结论 = MLX/MPS/Metal 均**同权重换框架**、不需 Mac 专用模型文件；mlx-audio 官方支持 Kokoro/Qwen3-TTS（高价值通道）；node-llama-cpp Metal 需 macOS 15+（14 静默失败）；Intel Mac 仅 CPU。全部 ⚠️ 待阶段 E 真机实测，仅作规划输入。来源见 §五 5.4。 |
