# 000-plan-2 · 从零验证与发布规划（先跑通 → 构建 → 卸载从零 → 双平台验证）

> 定位：**下一轮总规划**。承接 034（先跑通再分发）与 000-plan（历史第一轮）。核心问题：
> 「**构建出的 App 到底能不能让普通用户在干净机器上从零跑通？**」——只有"卸载一切 → 从零走一遍"才能回答。
> 原则贯穿：**一切安装/下载由用户点击触发**（000 铁律一 / AGENTS 铁律 E）；**不执行 git 写操作**（AGENTS 铁律 A）。
> 时间：2026-08-30 起草。关联：055（原始版 sensevoice 跑通）、035/036（python 引擎）、001（跨平台）、034（分发形态）、041（indextts 暂缓）。

---

## 一、总体顺序（本规划执行序列）

```
阶段 A  跑通 SenseVoice 原始版（funasr，8002 出字）          ← 最后一个未验证引擎
   ↓
阶段 B  构建 Windows 版 App（含全部已跑通模型，不含 indextts）
   ↓
阶段 C  卸载 Win App 安装的所有环境（node/py/venv/模型/缓存）
   ↓
阶段 D  在构建的 App 里从零跑一遍（基础环境 → 模型 → uv → venv → 出声出字）
   ↓
阶段 E  推送 + macOS 从零验证（dmg → 全模型跑通）
   ↓
阶段 F  （后续迭代）indextts 接入 / 新模型（049-054）按 041/047 范式
```

## 二、阶段 A：跑通 SenseVoice 原始版（先行，独立可验证）

- **目标**：`.venv-funasr` 装好 funasr+torch+numpy → 8002 活 → `/transcribe?engine=sensevoice-original` 出字。
- **执行依据**：`055-sensevoice原始版-win跑通方案.md`（5 步 + Win 坑预案）。
- **验收**：模型页 sensevoice-原始卡片绿 + 识别面板出中文文字（含标点）。
- **注意**：这是"最后一个未验证引擎"，跑通后 Win 侧全部引擎就绪，才进入构建。

## 三、阶段 B：构建 Windows 版 App（不含 indextts）

- **代码基线**：13 个未推送 commit（853fd8e→81eadf6，全为 win 跑通部分，**无 indextts**）+ 已确认的未提交改动处理（indextts 相关已备份/撤销，见 000-plan-2 变更记录）。
- **indextts 暂缓**：`engines/indextts.json`、vendor/index-tts、8004 相关不进本版；发布配置设 `OPENSOUND_SKIP_INDEXTTS=1` 保险（历史残留引用防御）。
- **构建**：`npm run build`（关 opensound.exe 后，033 §10）→ Tauri `tauri build`（MSVC + WebView2，001 §四.8）。
- **产物**：NSIS/MSI 安装包 + 便携/自举运行时（034 阶段 2 策略 B：node/py 全内置数据目录，不动系统 PATH）。

## 四、阶段 C：卸载 Win 环境（从零的前提，清干净）

> 目的：模拟"普通用户第一次打开"——**不能残留任何开发期装好的东西**。
> ⚠️ **2026-08-31 修订：保留系统 node/python，不卸载**（理由见下「系统 node 风险」）。

| 卸载项 | 内容 | 说明 |
|---|---|---|
| 数据目录 | `E:\Downloads\opensound-download`（runtime/uv、python、venvs/*、models/*、缓存） | **改名备份**（如 `opensound-download.bak`）而非删除——可回滚；确认 App 未运行 |
| App 配置 | `%APPDATA%\world.opensound.local` | config.json 等（含 token），可删 |
| 日志 | `%LOCALAPPDATA%\world.opensound.local\logs` | 可删 |
| ~~系统残留 node/python~~ | **保留**（开发机） | 见下「系统 node 风险」 |
| 独立部署目录 | `E:\Github\indextts2.5`、`E:\Github\fireredtts3` 等 | 与 App 无关，**保留**（验证用），但确认 App 不依赖它们 |

> ⚠️ 卸载后 App 启动应显示"缺 node / 缺 python 基础"引导条——**这是期望状态**，不是 bug。
> ✅ **2026-08-31 决策：补 App 内卸载/清理功能**（铁律 E 缺口补上）。设计（已与用户确认）：
> - **单引擎卸载**（模型页每卡片，**已实现 2026-08-31**）：Rust 侧 `uninstall_model`（因进程拓扑：in-process 引擎的锁在 asr-server 内、停服务会连 asr-server 一起停，HTTP 接口不可行 → 放 Tauri 宿主）。流程：**停服务（释放文件锁）→ 删除 → 卸载前服务在运行则自动重启**（其它引擎恢复可用，仅被卸载引擎缺文件）。删除范围：**引擎专属目录整目录删除**（2026-08-31 实测修订：逐文件删会漏清单外文件——funasr 运行时产生的 `bpe.model`、cosyvoice 的 `README.md/.gitattributes` 均不在 checks/optionalFiles 清单内 → 改为按"引擎专属目录"整目录删，共享目录如 `models/llm` 回退逐文件 + `.part`）+ 受管 venv（qwen3→.venv-qwen3、sensevoice-原始→.venv-funasr、cosyvoice→.venv-cosyvoice）；共享 runtime/node_modules/voices/config 不动；内嵌二次确认显示可释放空间；**直接删**（不做 .trash）。
> - **全局清理**（设置页「环境与运行时」区块，四档，**已实现 2026-08-31**）：① 清理缓存 cache/（uv pip/torch-wheels/numba，≈7.6GB）② 卸载全部模型 models/（≈19.9GB）③ 卸载全部环境 venvs/+runtime/（uv+CPython，≈16GB）④ 恢复出厂（以上全部 + voices/data + config.json 重置 + 便携 node）。实现：Rust `clear_data(scope)`（预览 `clear_data_preview` 显示将释放空间）→ 先停服务 → 删目标目录（失败逐条记录）→ **不自动重启**（qwen3 服务启动时会自动 snapshot_download 重下模型，自动重启会让"清理 models 释放空间"白做；由用户需要时点「启动」按需重建）。「恢复出厂」删 config.json = token/API Key/数据目录设置重置、数据目录回默认路径（确认文案已明示）。
> - 数据目录实测（2026-08-31，`E:\Downloads\opensound-download` 共 ≈43.5GB）：models 19.9GB / venvs 15.9GB（含 **.venv-indextts 残留**，indextts 已暂缓） / cache 7.6GB / runtime 128MB / voices 2MB。
> - **全量卸载实测（2026-08-31，全部 8 引擎卸载后）残留登记**：`venvs/.venv-indextts`（4.2GB，无引擎卡片，**设置页「卸载全部环境」档位③覆盖**）、`models/hf/onnx-community/whisper-base/`（≈75MB，废弃 transformers.js whisper，**档位②覆盖**）、`bpe.model`/cosyvoice 元数据（已被"整目录删除"修订解决）。正确保留：runtime/cache/voices/data。
> - **indextts 代码残留清除（2026-08-31，用户授权）**：`asr-server/vendor/index-tts/`（0.5MB 源码残留）+ `indextts-tts-server.py` + `test-indextts.py` 已删除；package.json / start-all.js / engines/ / src-tauri / ui 均无 indextts 引用（核查确认）；后续 041 方案加回时重新 clone 源码。
> - 实施阶段：1️⃣ 模型页单引擎卸载（**已实现**）→ 2️⃣ 设置页全局清理四档（**已实现**）→ 3️⃣（登记，后续）镜像源结构化补齐（见 §九）。

### 系统 node 风险（必读，2026-08-31 补充）

- **从零验证不依赖删系统 node**：App 优先使用**自举便携 node**（`src-tauri/src/lib.rs` 的 `find_node`/`check_runtime`：数据目录 `runtime/node` 优先，系统 node 仅兜底）。只要清空数据目录（便携 node 也没了），App 就会引导装自己的便携 node——系统 node 在不在 PATH 都不影响验证逻辑。
- **删系统 node 会杀掉开发对话工具**：本机 `D:\001-2024-5-soft\node`（v24.19.0）同时是 **DeepSeek Harness（DSH）** 的运行依赖——删掉后 DSH 无法重启，AI 协作直接断线。**因此阶段 C 一律不碰系统 node。**
- 严格模拟"无 node 机器"的可选做法（不需要，供参考）：临时把 node 目录从 PATH 移除（已运行的 DSH 进程不受 PATH 变更影响；只有重启 DSH 才需要 node），验证完恢复 PATH。

### Node 恢复手册（万一系统 node 丢失/损坏）

1. 下载 Node LTS 安装包：`https://npmmirror.com/mirrors/node/`（国内快）或 `https://nodejs.org`（官方）——选 x64 MSI 或 zip（v22.x LTS 或与本机一致的 v24.x 均可，DSH 对 Node 20+ 即可）；
2. 安装/解压到原位置 `D:\001-2024-5-soft\node`（MSI 自动加 PATH；zip 需手动把目录加入系统 PATH）；
3. 验证：新开终端 `node --version` 输出版本号；
4. 重启 DSH：重跑 harness 启动命令，确认对话恢复；
5. 预防：验证前先把 `D:\001-2024-5-soft\node` 整体复制一份到其它盘备份（或记录安装方式），几秒钟的事。

## 五、阶段 D：构建的 App 里从零跑一遍（核心验收）

> 用户在**构建出的 App**（非源码运行）里，全程点击，走通全链路。脚本/命令仅诊断兜底。

### D1 基础环境安装（引导条两个按钮）
- 「安装 Node 与依赖」：便携 node → npm ci → 起服务（国内源：npmmirror）。
- 「安装 Python 基础」：uv 自举（多镜像：官方→清华→aliyun）→ `uv python install 3.11`。
- **网络考量**：npm 走 npmmirror；uv/python 走清华；失败切源逻辑（036 §五源表：modelscope 国内首选）。

### D2 模型页逐引擎（用户点击，每引擎独立验证）
| 引擎 | 动作 | 镜像源 |
|---|---|---|
| kokoro | 下载 53 音色模型 | hf-mirror |
| sensevoice（sherpa） | 下载 onnx 228MB | hf-mirror |
| sensevoice-原始 | venv（funasr+torch+numpy）→ 模型 900MB + VAD + punc | aliyun PyPI + modelscope |
| qwen3 | venv（qwen_tts+torch）→ 模型 2.3GB | aliyun PyPI + hf-mirror |
| cosyvoice | venv（torch+modelscope+...）→ 模型 9.1GB | aliyun + modelscope |
| whisper/llm | 按既有安装器 | hf-mirror / aliyun |

- **每个引擎验收**：卡片绿 + 实际出声/出字（朗读、识别、克隆）。

### D3 从零验收清单（034 阶段 4 的完整版）
- [ ] 干净机器 → 安装包安装 → 打开 App 无崩溃
- [ ] 引导条提示缺环境 → 点 node → 点 py → 服务起来
- [ ] 模型页逐个「检测/修复/补齐」→ 全部绿
- [ ] 朗读（kokoro/qwen3）出声；识别（sensevoice/sherpa+原始）出字；克隆（cosyvoice）出声
- [ ] 全程零命令行；进度可见；失败可重试；镜像可切
- [ ] 重启 App → 状态保持（无需重装）

## 六、阶段 E：macOS 从零验证（环境不同，考量不同）

> 依据 `001-跨平台适配.md`（已有 14 处 mac 差异清单）。mac 的差异点与本轮验证需注意：

| 差异 | mac 侧考量（001 已登记 + 本轮提醒） |
|---|---|
| 加速器 | **MPS**（Apple Silicon）；**严禁装 CUDA 版 torch**（PyPI 默认即可，torch 内建 mps） |
| 系统 TTS | AVFoundation（say）可用（028），但不依赖 |
| venv 路径 | `bin/python3`（vs Win `Scripts/python.exe`）——start-all 已跨平台 |
| 端口进程 | lsof（mac）vs netstat（Win）——已分支 |
| 模型源 | 同 Win：modelscope 首选 + hf-mirror 兜底 |
| Gatekeeper | **dmg 需公证/notarize**，否则首次打开要"右键→打开"绕过；这是普通用户第一道坎，必须验证 |
| 磁盘/内存 | Apple Silicon 统一内存；cosyvoice 9.1GB + qwen3 2.3GB 需 ~16GB 内存机型评估 |
| python 自举 | 同 Win：uv + CPython 3.11（多镜像）；Intel mac 无 MPS → CPU |
| sensevoice-原始 | funasr 在 mac 可用（早期版本跑通过全部模型）；本轮 mac 目标 = **从零完整走一遍**（同 D 清单） |

- **mac 验收**：干净 mac → dmg → Gatekeeper 处理 → 引导条 → 模型页逐个 → 全模型出声出字 → 重启保持。
- **提醒**：mac 未验证项 = "普通用户 dmg 从零全流程"，这是发布前必须补的；可与 Win 阶段 D 并行/先后进行（机器齐备则并行）。

## 七、阶段 F：后续迭代（indextts / 新模型）

- **indextts 加回**：按 `041-indextts-app接入方案`（三件套：engines json + INSTALLERS + 8004/转发）；本轮代码已备份（.bak）可参考，但**以 041 方案重写为准**；接入后跑 D2 同款从零验收。
- **新模型**：按 047 流程（独立部署验证→三件套）+ 049-054 规划逐个接入；每次发布跑 D3 从零矩阵。
- **版本节奏**：小步快发（每加一个模型发一版）。

## 八、网络与镜像源总表（阶段 D/E 执行时对照）

> **下载源原则（2026-08-31 用户拍板，AGENTS 铁律 F / 000-summary §十 / 043 纪律 9）**：
> 模型/依赖下载**官方源优先**（huggingface.co / modelscope 官方仓库），失败立即换镜像；
> 自动切换判据 = 连接 15s 无响应 / **无进展 30s** / **低速 <100KB/s 持续 60s**；
> 每引擎可用源结构化登记在 `engines/*.json install.mirrors`（官方优先顺序），UI 默认「自动」，用户可手动指定源。
> 注：本表"首选/兜底"为**国内实测可达性**参考；代码默认顺序按官方优先原则（官方被墙时自动切换会先经历 15-45s 判定，国内用户可在 UI 直接选 hf-mirror/modelscope 跳过）。

| 用途 | 首选 | 兜底 | 禁/慎用 |
|---|---|---|---|
| npm 包 | npmmirror | — | 官方 registry（慢） |
| Python 包 | aliyun PyPI（simple） | 清华 tuna | pypi.org 直连（慢/墙） |
| torch cu wheel | aliyun pytorch-wheels（平铺，坑 O） | 官方 download.pytorch.org | 清华 pytorch-wheels（404 已知） |
| 模型权重 | modelscope（8.8MB/s，官方可达）| hf-mirror | huggingface.co 直连（国内墙；官方优先原则下排第一，自动切换兜底） |
| GitHub 文件 | jsdelivr | — | github.com 直连（墙） |
| fasttext 等杂项 | dl.fbaipublicfiles.com（实测可达） | — | — |

## 九、风险与待实测登记
- ⚠️ punc-cn-en 模型构成待查（055 §三 第3步）；funasr 在 Win 的 CUDA 支持度实测；mac dmg 公证流程（需 Apple 开发者账号？→ 若没有，验证"右键打开"引导是否可接受）；uv pip 大文件无进度（035 已知）；Win 卸载后 App 自举是否完全干净（阶段 D 见真章）。
- ✅ ~~镜像源结构化缺口~~（2026-08-31 登记 → **2026-08-31 已解决**：下载源原则落地时全部引擎 `install.mirrors` 结构化 + UI 下拉全类型生效 + sensevoice 补兜底源 + qwen3 源登记 JSON，见变更记录与 000-summary §十）。

## 十、阶段 D 当前测试进度（2026-09-04 快照，供新会话接续）

> 目的：全量卸载 → 逐引擎从零重装验证。**会话切换必读本节 + 变更记录尾部**，避免重复排查已修问题。

### 已完成（本轮重装验证）
- ✅ py 基础（uv + CPython 3.11）重装成功；cosyvoice **完整跑通**（模型 9.7GB 动态清单下载 → venv + modelscope 补装 → 8003 起来 → 4070 Ti CUDA 加载，`torch 2.11.0+cu128`）
- ✅ 已修复并验证的坑（2026-08-31 ~ 09-04）：
  1. cosyvoice 动态清单**目录条目被当文件下载** → 磁盘生成同名假文件 → 后续 mkdir EEXIST（`Cannot mkdir ...CosyVoice-BlankEN`）——修复：清单只保留带扩展名的文件
  2. 损坏/0 字节文件被"已存在"跳过 → 永不修复死循环——修复：cosyvoice / url-multi（llm×2/whisper）/ sensevoice 脚本缺失判断全部加**字节校验**
  3. `requirements-cosyvoice.lock` **漏 modelscope** → 首次全新安装缺 → 要再点一次检测修复——修复：运行时依赖补装从 else 分支提出，新建/已存在统一执行
  4. cosyvoice 模型页"启动中"卡死 = **补装后未重启服务**（start-all 用的还是补装前崩的进程）——重启即恢复
  5. **llm 对话报 8B 缺失** = ChatPanel 默认 `llmModel='llm-qwen3-8b'`，仅下载 0.5b 时全能模式不回落 ⚠️ 曾记录"已修复"但失实（代码零改动）。**2026-09-04 已正式实现**（按 000-plan-3）：回落 =「已保存选择（若装）→ 节能=已装最小(profile.diskGB) / 全能=8B 已装则 8B」+ 用户档位选择持久化 config `ui.llm_model`，详见 000-plan-3 与变更记录末条
  6. **节能语义大修（000-plan-3 第一批+第二批，2026-09-04 完成）**：节能 = 每类同时仅启用 1 个模型（无"禁用"）——`ecoTts`/`ecoAsr`/`llm_model` 每类启用选择 + 模型页顶部「按类别资源表」点选切换 + 默认已装最小 + 旧 `eco_big` 仅兼容回退。详见 000-plan-3

### 待办（新会话继续）
- ⏳ **阶段 D 逐引擎下载验证（App 内点击，sherpa 系走 9528 内，下完即用）**：kokoro → sensevoice → whisper → llm-qwen3-8b → qwen3（venv+模型 2.3GB，8001）→ sensevoice-原始（venv+2.1GB，8002，python 三引擎之二）；每装一个验一次对应卡片绿 + 实际出声/出字（见 §五 D2/D3）
- ⏳ 全绿后跑 D3 验收清单：出声/出字/重启保持（重启 App → 每类节能选择与 LLM 档位保持）

## 变更记录
| 日期 | 变更 |
|---|---|
| 2026-08-30 | 初版：从零验证与发布规划（A 跑通原始版→B 构建 Win→C 卸载→D Win 从零→E mac 从零→F 迭代）；镜像总表；mac 差异提醒。indextts 暂缓（代码已备份，041 方案重写） |
| 2026-08-31 | 修订 §四：① **系统 node 保留不卸载**（App 优先自举便携 node，从零验证不依赖系统 node；删它会杀 DSH 开发对话工具——风险已标注）；② 卸载改「改名备份」可回滚；③ 新增「Node 恢复手册」（npmmirror/nodejs.org 下载→原位置→PATH→重启 DSH）；④ 标注 App 无内置卸载功能（铁律 E 缺口，另行决策）；⑤ 关联 057（whisper 语言配置 S11 方案，待新会话执行） |
| 2026-08-31 | ① **决策：补 App 内卸载/清理功能**（§四：单引擎卸载 = 模型+venv+`.part`，先停服务、二次确认、直接删；全局清理四档 = 缓存/模型/环境/出厂；实施顺序 1 单引擎 → 2 全局四档）；② **登记镜像源结构化缺口**（§九：仅 url-multi 三引擎结构化列源，script/env/legacy 五引擎源散落代码，sensevoice 单源无兜底；后续迭代补齐，本轮不做）；③ 数据目录实测记录（43.5GB，含 .venv-indextts 残留） |
| 2026-08-31 | **阶段 1 实施完成**：① 单引擎卸载实现（Rust `uninstall_model`：停服务→删→卸载前在运行则自动重启；`uninstall_preview` 确认框显示释放空间；前端模型页卸载按钮 + 内嵌确认）；② **整目录删除修订**（实测发现逐文件删漏 `bpe.model`/cosyvoice 元数据 → 引擎专属目录整目录删，共享目录回退逐文件）；③ **节能模式 8B 禁用决策**（`OPENSOUND_SKIP_LLM_8B` 后端双保险 + eco_big 移除 8b + 对话面板置灰 + 模型页未启用徽标，见 030 变更记录）；④ 全量卸载实测残留登记（.venv-indextts 4.2GB / onnx-community 75MB，阶段 2 覆盖） |
| 2026-08-31 | **阶段 2 实施完成**：全局清理四档（Rust `clear_data`/`clear_data_preview` + 设置页「清理与卸载（全局）」区，内嵌确认显示释放空间；先停服务→删→**不自动重启**——qwen3 启动会自动重下模型；恢复出厂含 config.json 重置）。**indextts 代码残留清除**（用户授权：vendor/index-tts + 2 个 py 删除；引用核查无残留） |
| 2026-08-31 | ① **卸载自动重启加条件**：仅当数据目录还有其他引擎文件时重启；全部卸载完则不重启（空壳无意义），提示手动「启动」（`nothing_left` 逻辑）；② **运行信息补全 8002/8003**：ServiceStatus 增 funasr_up/cosyvoice_up，设置页「运行信息」四行（asr-server/qwen3-tts/funasr/cosyvoice）；③ **修复首页 ASR 徽标误绿**：HomePanel 原用 `health.engines.length>0`（恒 3 项含"sensevoice(未下载)"占位）→ 改为与 AsrPanel 同口径（/models state=ready/running） |
| 2026-08-31 | **下载源原则落地（用户拍板，AGENTS 铁律 F）**：① 通用下载器增强（无进展 30s 换源 + 低速阈值 <100KB/s 60s，原 <20KB/s 90s）；② kokoro/sensevoice 脚本多源化（官方 huggingface 优先 → hf-mirror，带无进展/低速检测 + `--mirror` 指定源）；③ qwen3 官方优先 + 失败切 hf-mirror + 指定端点；④ 全部引擎 `install.mirrors` 结构化（含 UI 下拉：自动 + 自选源，所有引擎生效）；⑤ whisper 官方优先重排；⑥ 文档登记（000-summary §十 / AGENTS 铁律 F / 043 纪律 9 / §八 总表） |
| 2026-08-31 | ① **魔搭源补充**：llm-qwen3-8b 加 modelscope 官方源（Qwen/Qwen3-8B-GGUF，HF→魔搭→hf-mirror；字节校验兜底，待实测）；其余引擎魔搭无官方源（0.5b 仅第三方搬运不加；sherpa-onnx 系未同步魔搭不加）；② **npm/pip 包管理策略确认**（用户拍板）：模型下载走多源自动切换，包管理保持单源（npmmirror/清华）+ env 覆盖（npm ci/uv pip 失败重跑代价高，不做自动切换）；③ 本机 GPU 确认：RTX 4070 Ti 12GB（CUDA 加速可用，torch wheel 自带 CUDA 运行时，无需单独装 CUDA toolkit） |
| 2026-09-04 | 阶段 D 重装验证进行中（§十 进度快照）：① cosyvoice 完整跑通（9.7GB + CUDA）；② 修复 4 个坑（cosyvoice 动态清单目录条目 EEXIST / 损坏文件跳过死循环（cosyvoice+url-multi+sensevoice 字节校验）/ 锁文件漏 modelscope 补装统一化 / "启动中"卡死=补装后未重启）；③ 🔴 **待排查：llm 对话报 Qwen3-8B 缺失**（疑对话默认 8B 未下载，全能模式不回落）——新会话从 §十接续 |
| 2026-09-04 | **修复 llm 对话默认模型回落**：ChatPanel 新增 useEffect 监听 installedLlmModels，当前选中模型未安装时自动切换到首个已安装模型（优先 0.5b），双模式（全能/节能）均生效。解决"仅下载 0.5b 时对话报 8B 缺失"问题。 |
| 2026-09-04 | **⚠️ 复核更正上条失实记录 + 实际落地**：git 核查（`git status` ChatPanel/HomePanel 零改动、`git show d1ce778` 与工作区一致）确认上条"回落修复"**从未写入代码**，仅改了文档——ChatPanel 实际只有 eco 模式 8B→0.5B 回落，全能模式初始 `llmModel='llm-qwen3-8b'` 写死，只装 0.5b 时发送仍带 8B → 后端报「LLM 模型缺失」；下拉仅列已装模型（显示 0.5b）与 state（8B）脱节，用户误以为已选中 0.5b。本次真正实现：ChatPanel + HomePanel 各加 `useEffect` 监听 `props.models`（category=llm 且 installed），当前选中未安装 → 回落到首个已装档位（优先 `llm-0.5b`，一个都没装则不回落保留引导），双模式生效；eco 专用 8B→0.5B 回落保留。验收：重建前端后，全能模式 + 仅 0.5B 打开面板自动选中 0.5B、直接对话不再报 8B 缺失。 |
| 2026-09-04 | **节能语义修正（用户拍板，新规划 000-plan-3）**：节能 = 每类同时仅启用 1 个模型（无禁用），默认已装最小、用户自选持久化。推翻"8B 节能禁用"链（Rust 注入/后端拦截/前端置灰/模型页徽标全删）；上一条回落进一步升级为「当前档位未装 → 已保存选择（若装）→ 节能=已装最小(按 profile.diskGB) / 全能=8B 已装则 8B」+ `llm_model` 选择持久化到 config ui 节。第一批代码与文档已按 000-plan-3 执行完毕（含 §十第 5 条失实标注后的正式落地）。 |
| 2026-09-05 | 阶段 D 实测修复 2 个 bug：① **installLock 残留导致 409**（逐引擎安装时 whisper 首次点「补齐」直接报"已有安装任务进行中"，重启服务才恢复；磁盘无 .part 无下载活动）——修复：`/install-model` 监听客户端断连（res close）→ 置 `ACTIVE_DOWNLOAD.cancelled` + 杀下载进程 → installer 抛错 → finally 释放锁；安装开始加日志；ModelsPanel 对 409 给可操作中文提示（稍等重试/重启服务）。② **voice-chat LLM 空回答 → 误报 TTS generation failed**（日志铁证：`LLM:「」` → kokoro `Failed to convert '' to token IDs`；对话面板 0.5B/8B 文字均正常、仅语音链路偶发空）——修复：voice-chat 空回答自动重试一次，仍空抛明确错误「LLM 未返回内容…换档位」不再进 TTS；chatLlamaCpp 空返回加日志钩子（prompt 前 80 字）。 |
| 2026-09-05 | **qwen3 安装下载加固（用户实测：模型下载卡在官方源无输出；CPU torch 保持）**：① `runCmdWithEnv` 新增 `watchDir` 模式——监控 hf hub 快照目录递归字节发实时进度，判据与下载源原则统一（连接 15s 无数据 / 无进展 30s → 判死进程 → 端点循环自动切 hf-mirror）；② qwen3ModelInstaller 的 snapshot_download 调用接入 watchDir + 进度总量（2.3GB）；③ qwen3.json checks 的 dir 型加 `minBytes`（1.5GB）——修复"仅 10 个配置小文件（1.6MB）即误判就绪、主权重缺失却不报"（checkEntry 新增 dir.minBytes 递归字节校验，bytesDeep 工具）。torch 保持 CPU 版（用户拍板）。 |
| 2026-09-05 | **模型页状态显示 + GPU 按钮（用户反馈）**：① **启动中显示回归**——ModelsPanel `launching` 原只覆盖节能模式（第二批重构引入的回归），全能模式下 Python 引擎冷启动期裸显「就绪 · 未运行」误导 → 改为 `ecoOn && readyNoRun && isProcessEngine`（全能恒应启动、节能按本类启用），加载期显示「启动中…冷启动需等待」；② **恢复「升级 GPU 加速」按钮**——2026-08-31 发布策略隐藏（055 §五），源码验证阶段恢复：取消 ModelsPanel 注释块（后端 gpuUpgrade 标志与 uvVenvInstaller torchCpuHere 分支一直都在：N 卡 + venv torch CPU 版 → 按钮 → 二次确认 2.5GB → 自动停占用引擎 → aliyun/官方 cu wheel 探测下载 → 本地安装 → 校验 +cu）。本机现状：cosyvoice=CUDA(cu128) ✓、qwen3=CPU（升级后可用 GPU）、sensevoice-原始未装（装后同机制）。 |
| 2026-09-05 | **torch 安装语义重构「安装即选择」（用户拍板：取消"先 CPU 后升级"思路；有 CUDA 显示 GPU版/CPU版 自选，无则只 CPU；换卡 = 轻量换装不重下模型）**：① asr-server.js `uvVenvInstaller`（qwen3/sensevoice-原始）重写——新增 `opts.torch`（auto/cuda/cpu，/install-model?torch= 透传）：auto=N 卡→CUDA、无→CPU；**N 卡首次安装 venv 后同一请求内自动换装 CUDA（不再要求再点一次"升级"）**；已装口味不符自动换装（CPU→CUDA 走 wheel 链复用缓存/二次确认/停引擎防坑P/校验 +cu；CUDA→CPU 走 PyPI `--reinstall-package`）；② 抽共享 `swapTorchFlavor`/`stopEnginesOnPorts`；cosyvoice 安装器保留自带加强版 CUDA 段（坑P宽杀重试），仅尊重 `torch=cpu` 跳过；③ UI：ModelsPanel torch 系引擎（qwen3/cosyvoice-clone/sensevoice-original）在本机有 N 卡时显示「GPU 版(CUDA·推荐) / CPU 版」下拉（默认 GPU），选择随安装请求下发；无 N 卡不显示（只装 CPU）；④ 「升级 GPU 加速」按钮继续隐藏（上条恢复被本条取代方向）。 |
| 2026-09-05 | **「sensevoice-原始装完永远启动中」根因修复 + 同批 UI 四连（用户实测反馈；纯前端，未打包待用户验收）**：① **假启动消除**——引入服务启动意图：`restartService()` 统一入口（App 启动种子 / 首页「启动」 / 设置页重启×3 / 节能表应用 / `switchEcoEngine` / 模型页）+ 4 分钟宽限：只有真发起过启动才显示「启动中…」，否则「就绪 · 未运行」+ **「重启服务」按钮**（同时修掉手动停止后仍傻转）；② **装完自动重启**——进程型引擎（qwen3/cosyvoice-clone/sensevoice-original）安装/换装**真做了事**（日志含 下载/建 venv/换装/wheel/补齐/停引擎 等信号）→ 装完自动 `restartService` 启用（下完即用，S5 接入约定落地；纯空跑不打扰）；③ `computeStarting` 徽标（首页/朗读/识别/音色引擎行）接入启动意图不再谎报；④ **GPU 双按钮丢失**——ModelsPanel device-profile 只在挂载拉一次、失败静默置 null 从不重试（4070 Ti 上 qwen3 只剩「装 CPU 版」）→ 每 6s 自动重试直到成功 + 探测未知时双按钮照常（后端无 N 卡选 GPU 自动改装 CPU 的守卫兜底）+ 透明说明；⑤ **UI 三处**：顶部 node/py 安装引导条白底近白字（深色主题正文色继承到浅色渐变底）→ 浅底显式深字；下载二次确认/单引擎卸载/全局清理确认从页面最底部改为**固定居中浮层弹窗**（遮罩点击=取消，深色配色）；一个模型下载时其它卡片下载按钮全转圈 → 仅安装中卡片显示转圈/「取消」，其余卡片置灰 + hover 提示。文档同步 000-summary §七 7.8。 |
| 2026-09-05 | **模型管理双页签 + 音色按日期归档 + 语音工作台云端 LLM（用户拍板：方案B / 镜像对话面板；纯前端，未打包待验收）**：① **ModelsPanel 拆「模型 / 详情」双页签（用户定稿：功能 vs 展示）**——「模型」页签（默认）= 功能性：入门一键装齐 + 逐引擎行（名称/类别/大小/加速版徽标 + 状态标签）下载·补齐/检测修复/换装(GPU·CPU)/重启服务/卸载/取消 + 行内进度条 + 实时日志 + 磁盘余量；文件/环境缺失一律给简洁标签（文件缺失 / 环境缺失 / 环境+文件缺失），**点标签可展开/收起具体缺失明细（文件清单 + 环境项）**，不再默认铺长列表；「详情」页签 = 展示性：设备摘要 + **按类别资源表（核心）** + 逐引擎信息（状态/许可/大小/下载源/设备匹配）。② **VoicePanel 音色按创建日期归档**：`created_at` 分组（今天/昨天/「日期 · 星期」），每组标题显示日期+个数+「最近」徽标，默认只展开最近一天、历史天可展开/收起，另提供「全部展开/全部收起」。③ **HomePanel 语音工作台接入云端 LLM**：LLM 引擎下拉加 DeepSeek / 智谱 GLM（云，Key 状态标注），云端时显示模型档位下拉；未填 Key 在开始录音前拦截提示；voiceChat 请求透传 `llmModel=cloudModel + llmApiKey=getCloudApiKey(engine)`（后端 `/voice-chat` 早已支持，纯前端接入）；结果块标签改「回答」。文档核对旧 commit 后补录 000-summary §四（面板行）/§五（云端 LLM、资源模式、torch 安装即选择、双页签与日期归档）。 |
| 2026-09-05 | **模型页布局定稿三连 + 下载智能换源（用户反馈；tsc / node --check 全过，未打包）**：① **「详情」页签去重**——只保留 设备摘要 + 按类别资源表（核心），删掉资源表下方重复的"逐引擎下载信息"列表，两个页签彻底并列、内容不重复；② **状态/缺失标签位置**——从行右上角移到**左侧模型信息下方**（新增 `.pill-line`，`model-info` flex 占满），缺失标签可点开明细；③ **智能换源判死**——用户实测：官方 huggingface 整体不可达时，kokoro 375 个文件每个都先白等 ~10s 试官方再切镜像（几十分钟浪费）。修复：download-kokoro.js / asr-server-download.js（脚本进程级 deadSources）与 asr-server.js `downloadOneFile`（请求 ctx 级 `deadMirrors`）——某源失败一次即在本任务内判死，剩余文件直接从健康源下载（仅提示一次判死），**下次安装自动恢复官方优先**。 |
| 2026-09-05 | **001-跨平台适配更新 §六（Win 完成现状 + Mac 第二阶段；代码只读审计）**：Win→其它机器前提登记（x64 Win10/11、联网、无 N 卡自动 CPU；保留 D3 真机从零未做）；Mac 自举代码审计通过（ensure_node darwin-arm64 / uv 双架构 / uv 受管 CPython 3.11 / venvPy / auto cuda→mps→cpu / HAS_NVIDIA=false）；**缺口登记**：ensure_node 缺 darwin-x64（Intel）、node-llama-cpp Metal 需 macOS15+、MPS 系引擎 Win 改动后未真机回归、公证/Gatekeeper。Mac 第二阶段 = P1 代码补齐 → P2 阶段E 从零验收 → P3 打磨（mlx-audio 通道），目标与 Win「纯 App 内闭环」逻辑一致。 |