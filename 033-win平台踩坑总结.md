# 033 · Windows 平台开发踩坑总结（Win 实测实录）

> 目的：把 Win 移植/开发过程中**真实踩过并已解决**的坑记录下来，避免重蹈覆辙；每条附"现象 → 根因 → 修复/规避"。
> 口径：2026-08-27 本机实测（Win10/11 x64 + Node v24.19.0 + Python 3.12.3 + WebView2）。

---

## 一、环境与启动链路坑

### 1. asr-server/node_modules 缺失 → 界面全线空白
- **现象**：模型页"暂无模型数据（请确认服务已启动）"、磁盘"未知"；服务 9528 起不来。
- **根因**：asr-server 的依赖从没安装过（`node_modules` 不存在），后端一启动就崩。
- **修复**：App 内一键自举装依赖（032）；同时 **P1 把模型清单/磁盘改为本地直取**，不再依赖 9528（服务挂界面也正常）。

### 2. npm ≥11 默认拦截 install 脚本（allowScripts 策略）
- **现象**：`npm ci` 后 node-llama-cpp / onnxruntime-node 等**原生二进制没下载** → LLM / whisper 引擎静默不可用。
- **根因**：npm 11 起默认拒绝未批准的 install/postinstall（`npm install-scripts ls` 可见）。
- **修复**：`asr-server/.npmrc` 写 `allow-scripts=true`；实现侧用 `npm install-scripts approve <pkgs>` + `npm rebuild` 兜底。
- **参考**：npm/cli #9415、npm v12 将默认全拦截。

### 3. sherpa-onnx 的 Win 包名（修正 001 文档过时情报）
- **现象**：001 文档建议在 optionalDependencies 加 `sherpa-onnx-win32-x64`。
- **实测**：该包在 npm **不存在（E404）**；正确包名是 **`sherpa-onnx-win-x64`**，且 **`sherpa-onnx-node@1.13.4` 自带全平台 optionalDependencies**（win/linux/darwin × x64/arm64），Win 上 `npm ci` 自动装，**无需手动加任何包**。已回写 001 变更记录。

### 4. spawn 弹黑色控制台窗口
- **现象**：GUI App 每次拉起 node/npm/tar/taskkill 都闪黑窗。
- **根因**：node.exe 是控制台程序，父进程无控制台时默认新建。
- **修复**：Rust 侧统一 `creation_flags(CREATE_NO_WINDOW)`（lib.rs 的 `quiet()`）。

### 5. start-all spawn 不存在的 venv → 整个启动链崩溃
- **现象**：9528 反复起不来、点"启动"没反应。
- **根因**：`spawn(.venv-qwen3/Scripts/python.exe)` 触发 `error` 事件，`run()` 没监听 → **uncaught exception 把 start-all（连带 9528 判定）带崩**。
- **修复**：监听 `error` 事件 → 打印并跳过该服务，其余不受影响。

### 6. engines/*.json 的 runtime 路径写死 `bin/python3`
- **现象**：Win 上 venv 实际是 `Scripts/python.exe`，引擎永远报"缺环境"。
- **修复**：`runtimePathExists()` 跨平台判定（bin/python3 ↔ Scripts/python.exe）。

## 二、数据与磁盘坑

### 7. 磁盘显示"6GB"但 E 盘明明有 40GB
- **现象**：设备画像显示磁盘可用 6GB；用户实际 E 盘 40GB 空闲。
- **根因**：磁盘探测的是**数据目录所在盘**——默认数据目录在 `%APPDATA%`（**C 盘系统盘**，剩 6GB）。
- **修复**：P3 数据目录默认改为 `<下载目录>/opensound-download`，设置可任意改盘；磁盘探测（模型页底部 + 设备画像）统一按数据目录盘。

### 8. 下载中断留下的"存在但损坏"文件 → sherpa 原生崩溃
- **现象**：kokoro voices.bin 只下了 3.2MB/27MB；每次启动加载 → **sherpa native abort → 9528 进程摔死**；且"补齐"永远没用。
- **根因**：① 下载脚本无字节校验，`existsSync 即跳过`；② `download-kokoro.js` 有"model.onnx 存在就算完成"的短路；③ `kokoroReady()` 只看存在不看大小。
- **修复**：三处都加字节校验（`kokoroReady` / `downloadFile` / 完成短路）；损坏文件按"缺文件"对待，一键补齐删除重下。

### 9. 就绪清单与下载脚本不一致 → "永远缺文件"死循环
- **现象**：kokoro 一直缺 `phone-zh.fst` 和 `dict/`，补了也没用。
- **根因**：engines/kokoro.json 要求这两项，但 download-kokoro.js 的 SKIP/过滤**故意跳过**它们。
- **修复**：下载脚本与 engines 清单对齐（移除误跳），补下全部就绪项。

## 三、工具链坑

### 10. 构建时 exe 被占用
- **现象**：`tauri build` 报 `failed to remove file opensound.exe (拒绝访问)`。
- **根因**：正在运行的 App 占用 exe，Windows 不允许覆盖。
- **规避**：构建前关闭 App（本会话约定：**不再自动杀进程**，由用户关闭）。

### 11. PowerShell 5.1 读无 BOM 的 UTF-8 脚本 → 中文乱码/解析失败
- **现象**：`download-all-models.ps1` 报"字符串缺少终止符"。
- **根因**：PS 5.1 默认按 ANSI/GBK 读无 BOM 的 UTF-8 文件，中文乱码破坏引号配对。
- **修复**：脚本文件写 UTF-8 **带 BOM**（`EF BB BF`）。

### 12. pwsh 里 curl.exe 传 JSON 参数转义坑
- **现象**：`curl -d '{"text":"你好"}'` 服务端收到"请求体必须是 JSON"。
- **根因**：PowerShell 传参给原生 exe 的引号处理会吃掉 JSON 里的引号。
- **规避**：用 `Invoke-WebRequest -Body (ConvertTo-Json)` 或 `-d @file.json`。

### 13. WebView2 自动播放拦截 → 朗读"生成成功但没声音"
- **现象**：/speak 返回有波形（实测 89% 非零样本）、前端 `el.play()` 也被吞错 → 界面显示完成、无声音。
- **修复**：tauri.conf.json window 加 `additionalBrowserArgs: "--autoplay-policy=no-user-gesture-required"`；播放失败不再静默（回调 UI 提示）。
- **旁证**：本机最终无声原因是蓝牙耳机连接问题（非代码）。

### 14. 关闭窗口语义（Win vs Mac）
- **现象**：Win 关窗口后服务停、再开要等；Mac 点红点其实只是最小化。
- **约定（拍板）**：Win 关窗口=退出并停服务；Mac 由 ⌘Q 决定。注释已写清（lib.rs on_window_event），勿再改。

## 四、环境（开发/运维侧）

### 15. 别用"杀全部 node"做清理（会杀 DSH 自身）
- 教训：`Stop-Process -Name node` 会把运行 DSH 的 node 一并杀掉，会话直接中断。**清理只能针对明确 PID 或交用户执行**。

---

## 五、变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-27 | 初版：Win 实测坑 15 条（安装/依赖/数据/工具链/环境五类）。 |