# 009 云端语音 API 与系统语音引擎规划

> 背景：LLM 环节已完成本地（llama-cpp / Ollama）+ 云端（DeepSeek / 智谱 GLM）并列（见 007、008 及本次改造）。
> 本文回答两个问题：
> 1. 语言朗读（TTS）与语音识别（ASR）是否已有云端 API 支持？没有的话怎么加？
> 2. macOS / Windows 系统自带的语音引擎能否调用？怎样调用？

---

## 一、现状盘点（本项目）

| 环节 | 引擎 | 类型 | 位置 |
|---|---|---|---|
| ASR | SenseVoice（sherpa 量化版 / funasr 原始版） | 本地 | asr-server（8002） |
| ASR | Whisper（transformers.js） | 本地 | asr-server |
| TTS | Kokoro | 本地 | asr-server 内嵌 |
| TTS | Qwen3-TTS | 本地 | Python 子服务（8001） |
| TTS | CosyVoice3 克隆音色 | 本地 | Python 子服务（8003） |
| LLM | llama-cpp / Ollama / DeepSeek / 智谱 GLM | 本地+云端 | asr-server |

**结论：ASR / TTS 目前 100% 本地，尚无云端支持。** LLM 的「引擎注册表 + 前端选 Key」模式已经跑通，语音可以照搬同一套架构。

---

## 二、云端语音 API 现状（2026-08 核实）

### 2.1 DeepSeek：没有 ASR / TTS

DeepSeek 官方 API 只有文本模型（`deepseek-v4-flash` / `deepseek-v4-pro`，Chat Completions 与 Responses 接口），**不提供语音识别或语音合成端点**。若要云端语音，只能接第三方（火山豆包、讯飞、Azure 等），与本项目的「DeepSeek 直连」思路不匹配。

### 2.2 智谱：全家桶齐全（推荐首选）

智谱 BigModel 平台提供完整语音产品线，且都是 OpenAI 兼容格式：

| 能力 | Model ID | 端点 | 说明 |
|---|---|---|---|
| ASR | `glm-asr` / `glm-asr-2512` | `POST /api/paas/v4/audio/transcriptions` | multipart 上传音频，支持流式（SSE）；中英混、8 种方言、热词；约 0.06 元/分钟 |
| TTS | `glm-tts` | `POST /api/paas/v4/audio/speech` | 7 种音色，24kHz WAV/PCM，首帧延迟 ≤400ms |
| 声音克隆 | `glm-tts-clone` | `POST /api/paas/v4/voice/clone` | **3 秒样本即可克隆**（对比本项目 CosyVoice 需 3–10s 录音样本） |
| 实时语音对话 | `glm-realtime(-flash/-air)` | WebSocket | 支持打断、函数调用，Flash 约 0.18 元/分钟 |

Key 复用现有设置面板的智谱 Key（`zhipuKey`），无需用户重复申请。

### 2.3 其他可选（备查）

- 火山引擎豆包：TTS 音色最丰富（100+），有实时端到端语音大模型（WebSocket 二进制协议）
- 讯飞 / Azure / OpenAI TTS/Whisper API：成熟但付费门槛或网络问题

---

## 三、接入方案设计（建议按此实施）

沿用 LLM 的「请求透传 apiKey + 引擎路由」架构，改动集中在 `asr-server.js`：

### 3.1 云端 ASR：新增引擎 `zhipu-asr`

```
POST /transcribe?engine=zhipu-asr
```

1. 服务端把收到的 16kHz WAV 以 `multipart/form-data` 转发到
   `https://open.bigmodel.cn/api/paas/v4/audio/transcriptions`，字段 `model=glm-asr`、`file`
2. Header 带 `Authorization: Bearer <zhipuKey>`（query 参数传入或环境变量 `ZHIPU_API_KEY`）
3. 解析响应 `{ text }` 直接返回，后处理（标点）逻辑复用现有管道

Node 20+ 可用原生 `FormData` + `Blob` 构造 multipart，无新依赖。

### 3.2 云端 TTS：新增引擎 `glm-tts`

```
POST /speak?engine=glm-tts
```

1. `POST /audio/speech`，body `{ model:"glm-tts", input:<text>, voice:<音色>, response_format:"wav" }`
2. 返回的 WAV 走现有帧流协议（4 字节长度 + WAV 帧）下发前端，播放/保存链路零改动
3. 注意采样率是 24kHz：现有 `mergeWavFrames` 按 16kHz 拼接保存，需要按帧头实际采样率处理或统一重采样

### 3.3 前端改动（最小）

- 设置面板：复用智谱 Key（可选加一个开关「语音也走云端」）
- AsrPanel / HomePanel / RealtimePanel / ReadPanel 的引擎下拉追加云端选项
- 未填 Key 时选项显示「未填Key」提示（与 ChatPanel 一致）

### 3.4 不建议现在做的

- GLM-Realtime 实时对话：WebSocket 协议与现有 HTTP 帧流架构差异大，且本地 Realtime 链路已可用；留待后续版本评估
- DeepSeek 侧语音：官方无能力，不接第三方则跳过

---

## 四、系统自带语音引擎（问题三解答）

可以调用，而且成本极低——它们是操作系统内置的离线 TTS，通过命令行即可驱动，适合作为**零依赖兜底引擎**。

### 4.1 macOS：`say` 命令（底层 NSSpeechSynthesizer / AVSpeechSynthesizer）

系统自带，中文有婷婷（Tingting）、美佳（Meijia）等声音（Big Sur 起部分声音需在系统设置里下载一次）：

```bash
# 列出可用声音（找 zh_CN / zh_TW）
say -v '?'

# 合成到文件：指定 16kHz 16bit 单声道 PCM WAV（本项目标准格式）
say -v Tingting -o /tmp/out.wav --data-format=LEI16@16000 "你好，我是系统语音"
```

要点：
- `--data-format=LEI16@16000` 直接产出本项目期望的格式，免转码
- 无流式输出，长文本延迟高，适合短句朗读兜底
- 也可用 `osascript -l JavaScript` 调 AVSpeechSynthesizer 获得更新的增强型声音，但复杂度更高，不建议首选

### 4.2 Windows：SAPI5（PowerShell 调 System.Speech）

Win10/11 自带，中文声音（Huihui 慧慧等）随语言包装好即有：

```powershell
Add-Type -AssemblyName System.Speech
$s = New-Object System.Speech.Synthesis.SpeechSynthesizer
$s.SelectVoice((($s.GetInstalledVoices() | Where-Object { $_.VoiceInfo.Culture -like "zh*" } | Select-Object -First 1).VoiceInfo.Name))
$s.SetOutputToWaveFile("C:\temp\out.wav")   # 默认 16kHz 16bit 单声道
$s.Speak("你好，我是系统语音")
$s.Dispose()
```

要点：
- 由 asr-server spawn `powershell.exe -Command ...` 执行，WAV 天然落盘
- 声音质量一般，但完全离线、零下载

### 4.3 Linux（顺手支持）：`espeak-ng` / `speech-dispatcher`

```bash
espeak-ng -v cmn -w /tmp/out.wav "你好"
```

### 4.4 在本项目落地的方式

在 `asr-server.js` 的 `TTS_ENGINES` 注册表新增 `system` 引擎：

1. 按平台分发：darwin → `say`；win32 → `powershell`；linux → `espeak-ng`
2. 文本写入临时文件避免命令行转义问题（尤其引号、换行）
3. 子进程产出的 WAV 读入 Buffer，走与其他引擎一致的 `wav()` 返回协议
4. 启动时探测一次命令是否存在（`which say` 等），不存在则该引擎标记 unavailable
5. 前端朗读下拉追加「朗读： 系统语音」

预期工作量：服务端约 60–80 行，前端一处下拉项。**这是所有 TTS 选项里唯一零模型、零下载、离线可用的**，非常适合作为首次启动（模型未下载完）时的默认兜底。

### 4.5 系统 ASR？

macOS / Windows 都没有可直接命令行调用的听写接口（macOS 听写是系统 UI 层能力；Windows SpeechRecognition COM 质量差且配置繁琐），**不建议做系统级 ASR**，识别继续用 SenseVoice/Whisper（本地）或 GLM-ASR（云端）。

---

## 五、优先级建议

| 优先级 | 事项 | 理由 |
|---|---|---|
| P0 | 智谱 glm-tts（云端朗读） | 复用已有 Key，音质远超本地小模型 |
| P0 | 系统语音引擎 system（本地兜底） | 零依赖、首启动即可朗读 |
| P1 | 智谱 glm-asr（云端识别） | 方言/噪声场景补强本地 SenseVoice |
| P2 | 智谱 glm-tts-clone（云端克隆） | 3 秒样本，体验优于本地 CosyVoice 流程 |
| P3 | GLM-Realtime 实时语音对话 | 架构差异大，单独立项 |

> 信息核实时间：2026-08。模型 ID 与价格可能变动，实施前以 docs.bigmodel.cn 为准。
