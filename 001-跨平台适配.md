# 031 · 跨平台适配（Win / Mac 差异、注意事项与共性目标）

> 回答一个问题：**"同一套代码如何在 macOS 与 Windows 上都开箱即用？"**——本文盘点当前代码里所有 macOS 专属依赖，给出逐项适配方案与可能踩的坑，并明确产品的共性目标：**模型管理界面一键安装 → 重启即可用；Python/Node 环境等一切依赖由 app 内部自举，用户无需懂环境**。
> 口径更新（2026-09-05）：Win 侧已完成「App 内一键自举（node/py/引擎 venv）→ 模型下载/卸载/清理」全部闭环，并在本机做过「卸载 → 从零」验证（000-plan-2 阶段 C/D，阶段 D 基本完成）；本文 §六 为本轮后对 Win 交付前提与 Mac 第二阶段的结论与建议。
> 关联文档：`002-plan`（本地服务自举与分发）、`030`（服务资源模式）、`028`（Mac/Win 系统 TTS 与 Tauri 调研）。

---

## 一、共性目标（一切适配围绕它）

> **在模型管理界面安装模型 → 点击重启 → 立即可用。安装器负责模型权重 + 运行时环境（venv / 二进制）的全部自举，用户不需要装 Python、不需要配 PATH。**

- macOS 已基本达成（S4/S5：cosyvoice 安装器自动建 venv + clone 源码；`030` 资源模式管理启动）。
- Windows 适配的目标：**同一套安装器/启动器逻辑跨平台可用**，差异只收敛到"平台分支"。
- 验收标准：Win 全新机器装 app → 模型管理页安装 SenseVoice/Kokoro/CosyVoice → 重启服务 → 全部亮绿，无需命令行。

---

## 二、当前代码的 macOS 专属依赖（已核实清单）

| # | 位置 | macOS 写法 | Windows 问题 | 适配方案 |
| --- | --- | --- | --- | --- |
| 1 | `start-all.js` L24-26 | venv 路径 `bin/python3` | Win 是 `Scripts/python.exe` | 平台分支路径助手（见 §三） |
| 2 | `start-all.js` L111 | `lsof -ti tcp:<port>` 查端口进程 | Win 无 lsof | `netstat -ano` + `taskkill`，或 node 探测 |
| 3 | `start-all.js` L29-39 | `detectSysPython()` 探测 `/opt/homebrew|/usr/local|/usr/bin` | Win 用 `py -3` / PATH | 平台分支 |
| 4 | `start-all.js` L74 | 子进程 env 透传 | 基本兼容 | — |
| 5 | `asr-server.js` /disk | `df -k` 读磁盘 | Win 无 df | PowerShell 或 node `statfs`（mac 也有）/ `fs.statfs`（Node 18.15+） |
| 6 | `device-profile.js` | `df -k`、`sysctl` | `df` 无、`sysctl` 无（已 try/catch） | 磁盘用 `fs.statfs`；`sysctl` 保持 catch |
| 7 | `device-profile.js` | darwin → accel=metal | Win 走 nvidia-smi 分支（已有） | `nvidia-smi` Win 在 PATH，OK；⚠️ 需实测 |
| 8 | `cosyvoice-tts-server.py` | `--device` 默认 mps | Win 无 mps | 自动检测：cuda → mps → cpu |
| 9 | `qwen3-tts-server.py` | MPS 设备检测 | Win 需 cuda | 自动检测 |
| 10 | `sensevoice-server.py` | `--device` 默认 cpu | 可跑（慢） | 可加 cuda 分支 |
| 11 | `asr-server/package.json` | `optionalDependencies: sherpa-onnx-darwin-arm64` | Win 需要 `sherpa-onnx-win32-x64` | 加 Win optional 包 |
| 12 | `find_node()`（lib.rs） | 查 nvm / homebrew | Win 查 `nvm-windows` / PATH | 平台分支 |
| 13 | `.venv-*` 自建（cosyvoice installer） | `bin/python3 -m venv` | Win 路径 `Scripts` | 路径助手 |
| 14 | Tauri 构建 | macOS bundle | Win 需 MSVC + WebView2 | 标准 Tauri Win 流程 |

---

## 三、适配方案（分阶段）

### 阶段 A：跨平台命令与路径助手（启动器层）

1. **`start-all.js` / 各处 venv 路径**：新增平台分支——
   ```js
   const IS_WIN = process.platform === 'win32';
   const venvPy = (name) => IS_WIN
     ? path.join(__dirname, name, 'Scripts', 'python.exe')
     : path.join(__dirname, name, 'bin', 'python3');
   ```
   `PY`、`PY_COSYVOICE`、`PY_FUNASR` 全部改由 `venvPy(...)` 派生。
2. **磁盘探测（`/disk` + device-profile）**：弃 `df -k`，改用 Node 内置 `fs.statfsSync(path)`（Node ≥18.15，mac/win 都有）：
   ```js
   const st = fs.statfsSync(dir);
   const availBytes = st.bavail * st.bsize;
   ```
3. **端口进程检测（start-all 自愈）**：Win 用 `netstat -ano -p tcp | findstr :<port> LISTENING` 取 PID，杀用 `taskkill /PID <pid> /F`；mac 保留 lsof。
4. **`detectSysPython()`**：Win 优先 `py -3` / `where python3`，再 PATH。

### 阶段 B：Python 服务 device 自动检测

- `cosyvoice-tts-server.py` / `qwen3-tts-server.py`：`--device` 默认值改为自动检测（`torch.cuda.is_available() → cuda`，`mps.is_available() → mps`，否则 cpu），Win 天然落 cuda。
- `sensevoice-server.py`：保留 cpu 默认（功能不受影响），可选加 cuda。

### 阶段 C：依赖包与 Node 查找

- `asr-server/package.json` 增加 `sherpa-onnx-win32-x64`（optionalDependencies）——⚠️ 版本号需与 darwin 一致，实测登记。
- `lib.rs find_node()`：Win 分支查 `where node` / 常见 nvm-windows 路径。
- cosyvoice installer 的 venv 自建路径同 §三.A.1。

### 阶段 D：验证（⚠️ 无 Win 机器）

- 每项改动在 Mac 上回归（node --check + 起服务 + 冒烟），Win 侧由用户按本文档实测回填（007 纪律）。

---

## 四、可能出现的问题（注意事项）

1. **lsof/df/sysctl 缺失**是 Win 上第一坑：必须全部换成跨平台实现，否则 `start-all` 自愈失效、磁盘显示"未知"。
2. **venv 目录结构差异**（`bin/` vs `Scripts/`）：任何写死 `bin/python3` 的地方都会让 Python 服务起不来；用 §三.A.1 助手统一。
3. **Python 版本**：Win 用户不一定有 Python——按共性目标，安装器（/install-model）应像 cosyvoice 那样**自建 venv + 自动装依赖**（可用 `uv` 作为自举工具，`uv python install` 无需系统 Python）。⚠️ 分发策略待定（002 文档联动）。
4. **MPS→CUDA**：三个 Python 服务默认 mps 的，Win 上必须自动切 cuda，否则报设备错误。
5. **sherpa-onnx-node**：Mac 包与 Win 包不同（optionalDependencies 按平台安装），Win 上漏装会静默退化到 whisper 兜底——需要在 /models 或安装器里显式提示。
6. **Node 运行时**：Win 用户无 node 时同样依赖安装器自举（bundled node / uv 类似方案，002 文档）。
7. **路径分隔符**：所有 `path.join` 已跨平台；检查是否有字符串拼接 `/` 的遗留。
8. **Tauri Win 构建**：需要 MSVC + WebView2（Win10/11 自带），`tauri build --target x86_64-pc-windows-msvc` 标准流程；`Info.plist`/`windows` 配置分支。
9. **行尾/编码**：Python 脚本 UTF-8 + shebang 在 Win 无意义（用 `python.exe script.py` 调用，start-all 已是如此，OK）。
10. **IndexTTS（候选引擎）在 Win 反而更顺**：CUDA 原生 + `--all-extras` 可用（Mac 上装不了 CUDA 包是 Win 的优势）；本轮 Mac 侧已清理（磁盘 3.3→15.2GB），后续有空间再试 Mac MPS。

---

## 五、共性目标落地检查表（Win/Mac 通用）

- [ ] 模型管理页：安装按钮 → 自动下载权重 + 自建 venv/依赖 → 重启服务 → 徽标变绿
- [ ] 启动器（start-all.js）：跨平台命令（无 lsof/df/sysctl 依赖）
- [ ] Python 服务：device 自动检测（cuda/mps/cpu）
- [ ] 磁盘/设备画像：跨平台探测
- [ ] Node/Python 缺失时：安装器给出一键自举（而非报错退出）

## 五·补 macOS 从零验证提醒（000-plan-2 阶段 E，2026-08-30）

> 背景：Win 侧模型已全跑通（除 sensevoice-原始，见 055），即将构建 Win 版并做「卸载→从零」验证（000-plan-2 阶段 C/D）。
> **mac 侧从未做过「普通用户下载 dmg → App 内操作 → 全模型跑通」的从零流程验证**（早期版本开发者自跑通过，不等于从零可用）。
> 以下为 mac 从零验证时必须重点核对的项（多数已在 §二/§三登记，这里按"从零流程"视角重列）：

| # | 检查项 | mac 考量 / 验证动作 |
|---|---|---|
| 1 | **Gatekeeper / 公证** | dmg 首次打开可能被拦（未公证）。验证：① 有无 Apple 开发者公证流程；② 若无，确认「右键→打开」引导在普通用户视角可接受（产品决策） |
| 2 | 加速器 | Apple Silicon → **MPS**（torch 内建，PyPI 默认即可）；**严禁装 CUDA 版**（036 §六·A）；Intel mac → CPU（无 MPS） |
| 3 | venv 路径 | `bin/python3`（start-all `venvPy()` 已跨平台，回归确认） |
| 4 | 端口进程 | lsof 分支（mac）已实现，回归确认无 `netstat` 依赖混入 |
| 5 | 模型源 | 与 Win 相同：modelscope 首选 + hf-mirror 兜底（036 §五 源表） |
| 6 | python 自举 | uv + CPython 3.11（多镜像：官方→清华→aliyun）；mac 上 uv 是官方支持平台，验证 `uv python install` 走通 |
| 7 | 内存/磁盘 | Apple Silicon 统一内存：cosyvoice 9.1GB + qwen3 2.3GB + sensevoice-原始 900MB → 建议 ≥16GB 机型验证；磁盘余量 ≥30GB |
| 8 | 系统 TTS | AVFoundation（say，028 已实测）——兜底朗读可用，但不替代模型引擎 |
| 9 | 数据目录 | mac 上 `~/Library/Application Support/...`（与 Win `%APPDATA%` 不同）——卸载/从零验证时清这里 |
| 10 | 从零验收 | 与 Win 同清单（000-plan-2 §五.D3）：干净 mac → dmg → 引导条 node/py → 模型页逐个 → 全模型出声出字 → 重启保持 |

> 提醒：mac 的「未验证项」= 整个从零流程，不是个别模型；**发布前必须在一台干净 mac 上完整走一遍**（可虚拟化），再谈 mac 发布。

## 六、Win 已完成后的现状结论与 Mac 第二阶段对齐（2026-09-05）

> 本节回答两个直接问题并给出 Mac 第二阶段建议，口径 = **与 Win 已实现的"纯 App 内闭环"完全一致**（普通用户不碰终端）。

### 6.1 现状结论 A：Win 打包 App 换到其他 Windows 电脑能用吗？

- **设计目标已达成并部分验证**：App 内自举链不依赖系统环境——便携 Node 下载到 `%APPDATA%/runtime/node`（Rust `ensure_node`）、uv + 受管 CPython 3.11（`ensure_python_base`，不碰系统 python）、引擎 venv 全在数据目录 `venvs/.venv-*`、模型下载/卸载/全局清理全部在模型管理页内完成；本机「卸载 → 从零 → 逐引擎重装」已跑通（000-plan-2 阶段 C/D）。
- **换到"另一台陌生 Windows"的前提（非障碍，但发布前建议真机验一次）**：
  - 64 位 Win10/11（WebView2 自带）；无需管理员权限（便携 node/uv/python 都在用户目录）；
  - 首次需联网（node/uv/模型来自镜像，国内可达）；按所选引擎预留磁盘（设备画像会提示档位与缺口）；
  - **无 N 卡**：torch 引擎自动装 CPU 版（安装器 hasGpu=false → 单 CPU 版）；**有 N 卡**：GPU/CPU 双按钮可选，NVIDIA 驱动过旧会给明确提示；
  - 未签名安装包可能出现 SmartScreen/杀毒提示（发布前签名可消除）——与功能无关；
  - 唯一诚实的保留：**真正的"干净的第二台 PC 从零验收"尚未做**，D3 清单（出声/出字/重启保持/卸载）建议在发布前补一次。

### 6.2 现状结论 B：这份代码 repo 更新到 Mac 后能打包运行吗？

代码审计（2026-09-05，只读核实）结论：**跨平台自举机制在代码层面早已为 Mac 预留，且引擎侧无 CUDA 硬依赖**，但**自 Win 大量改动后 Mac 从未真机回归，Intel Mac 存在一个明确代码缺口**。

已核实为跨平台的部分（证据）：
| 位置 | Mac 分支 | 说明 |
|---|---|---|
| `lib.rs ensure_node` | `node-*-darwin-arm64.tar.gz`（npmmirror/nodejs.org） | 便携 node 平台化；⚠️ 见下方缺口 |
| `lib.rs uv_zip_name` | `uv-aarch64-apple-darwin` / `uv-x86_64-apple-darwin` | uv 双架构齐 |
| `ensure_python_base` | `uv python install 3.11`（UV_PYTHON_INSTALL_DIR=数据目录） | **受管 CPython，与系统 python 无关，版本与 Win 完全一致** |
| 引擎 venv | 服务端 `UV_EXE = <data>/runtime/uv`、`uv venv --python 3.11`；start-all `venvPy()` `bin/python3`↔`Scripts/python.exe` | Win/Mac 同构，数据目录 venvs |
| Python 引擎 device | `auto_device()`：cuda → mps → cpu（qwen3 / cosyvoice）；sensevoice-原始默认 cpu | Mac 自动走 **MPS**，无 CUDA 路径（`HAS_NVIDIA=false` → 永不走 CUDA 换装） |
| 依赖镜像 | `UV_INDEX`（tuna PyPI 全量镜像，含 mac wheel） | 同参数跨平台 |
| 模型 | 同一批权重文件 + 同一下载源/镜像/判死逻辑 | 与平台无关 |
| 应用数据 | `data_root`（mac=`~/Library/Application Support/…`） | 卸载/清理逻辑平台化 |
| 进程/端口 | stop/portPids 按平台（taskkill vs kill TERM / lsof） | 已有分支 |

**Mac 侧明确缺口（发布前需补）**：
1. **`ensure_node` 未处理 Intel Mac**：非 Windows 一律下载 `darwin-arm64`（lib.rs L864-868）；Intel Mac（x86_64）会拿到错误包 → 需按 `ARCH` 分支加 `node-*-darwin-x64.tar.gz`（或用系统 node 版本达标兜底）；
2. **macOS 15+ 门槛**（node-llama-cpp Metal 预编译，旧系统静默失败）——LLM 档位需标注/回落 CPU（000-plan-3 §五已登记）；
3. MPS 系引擎（qwen3 / cosyvoice 9GB）在本次 Win 改动后**未在真机回归**（MPS 并发锁、冷启动时长、显存=统一内存）；sensevoice-原始 funasr 在 mac 走 CPU；
4. 公证/Gatekeeper（同 §五·补 第 1 项）。

### 6.3 普通新 Mac 用户能全 App 内搞定吗？（回答）

- **能（按设计）**：node 下载、uv+CPython 自举、引擎 venv、模型下载/卸载/清理全部由 App 内按钮驱动，不依赖系统 python/node（mac 无需预装任何开发环境）；
- **py 版本一致性问题不存在**：Mac 与 Win 都使用 uv 受管的 **CPython 3.11**（同一数据目录方案），不读系统 python；node 用 App 内置便携版；
- **前提是补上 6.2 的缺口并在真 Mac 走通 6.4 从零验收**，否则只是"代码看起来能跑"。

### 6.4 Mac 第二阶段建议（阶段化，对齐 Win 已实现逻辑）

- **P1（代码补齐，改动小，发布前置）**
  1. `ensure_node` 增加 `darwin-x64`（Intel）分支；node 缺失时下载、系统 node 达标优先使用（与 Win 一致）；
  2. 全仓静态清点一遍平台分支：确认本轮 Win 改动（device-profile / install-model 安装器 / 卸载 / 清理 / 判死换源）无 win-only 依赖混入服务端与启动器（当前审计未见，仍建议 `node --check` + mac `cargo check` + tsc 过一遍）；
  3. mac 上确认 torch 安装走 CPU/MPS wheel（`HAS_NVIDIA=false`），**不出现** GPU/CPU 双按钮（UI 已有 CPU-only 分支）。
- **P2（= 000-plan-2 阶段 E：mac 从零验收，真机或 VM，发布门槛）**
  - 干净 Mac → dmg（含 Gatekeeper 处理）→ 引导条「安装 Node / 安装 Python 基础」→ 模型页逐引擎：
    kokoro → sensevoice（量化）→ whisper → llm-0.5b → llm-qwen3-8b → qwen3（8001，MPS）→ sensevoice-原始（8002，CPU）→ cosyvoice-clone（8003，MPS 9GB）；
  - 每装一个验卡片绿 + 实际出声/出字；重启 App 保持；卸载单引擎 + 全局清理四档复验；
  - 重点核对：MPS 并发 RLock 串行、cosyvoice 冷启动时长与内存（≥16GB 统一内存）、节能每类单开。
- **P3（打磨，排期后置）**
  - 云端 LLM（DeepSeek/智谱）等纯前端能力与平台无关，Mac 自动继承；
  - 若 MPS 表现差 → 000-plan-3 §五 已给出 mlx-audio 优化通道（独立新引擎，不动本轮逻辑）；
  - Intel Mac 仅 CPU：定位为低优先支持（LLM Metal 门槛 15+）。

## 六·补 产品主线阶段视图与阶段 4（Win 验证阶段 3 / S6′ 逻辑）指引（2026-09-08）

> 目的：把"产品推进主线"（不是上面的平台适配小阶段 A–D）讲清楚，说明当前架构在 Win 上应如何验证。进度见 000-summary §五·B。

### 6.5 四个阶段的口径

| 阶段 | 内容 | 状态 |
|---|---|---|
| **阶段 1：mac 开发** | 开发者模式：App 壳 + `server_path` 指向仓库 `asr-server`（代码在仓库跑），数据在数据根 | ✅ 完成（早期） |
| **阶段 2：Win 开发验证** | 同套代码在 Windows 本机开发验证可用（App 内一键自举 node/py/venv/模型闭环，见本文件 §六 2026-09-05 结论） | ✅ Win 本机验证（干净二台 PC 真机验收仍保留） |
| **阶段 3（当前）：功能修改 + S6′ 单产物自包含** | 若干功能增量 + **asr-server 代码内嵌进 .app**：不再需要 server_path 指向仓库；首启物化到 App 数据目录 `server/`；`.version` 指纹不符自动整目录重建；依赖仍由 App 内自举（见 002 §六 S6′） | ✅ 阶段 1–2 已在 mac 实测通过（2026-09-08，见 000-temp-test.md）；**Win 尚未验证 = 阶段 4** |
| **阶段 4：Win 上验证阶段 3 逻辑** | 在 Windows 重新构建并逐项复验 S6′（下述 §6.7） | ⏳ 待做 |

### 6.6 S6′ 逻辑的跨平台形态（代码层面已是平台无关，差异只剩路径/打包外壳）

| 环节 | mac | Win | 代码位置 |
|---|---|---|---|
| 内置模板生成 | 同一份 `scripts/stage-asr-server.mjs`（平台无关，每次构建现场生成） | 同左 | 仓库根 scripts/ |
| 内置进包 | `bundle.resources` → `Contents/Resources/resources/asr-server/` | 同配置 → 可执行旁 `resources/resources/asr-server/`（NSIS/MSI 打包） | `src-tauri/tauri.conf.json` |
| 物化目录 | `~/Library/Application Support/world.opensound.local/server` | `%APPDATA%\world.opensound.local\server`（Tauri `app_data_dir()` 自动区分） | `lib.rs materialized_server_dir` |
| 触发条件 | release 构建且无用户 `server_path` 配置；`.version`≠内置 → 整目录重建（dev 走仓库 exe 上溯，不受影响） | 同左（`#[cfg(not(debug_assertions))]` 与平台无关） | `lib.rs server_dir()` 分支 3 |
| npm ci 定位 | `npm_cli_js()` 多布局兜底（同级 node_modules → `bin/npm` 符号链接 → `lib|libexec/lib`） | Windows 官方 zip 布局 = 与 node 同目录 `node_modules/npm`（原假设成立） | `lib.rs npm_cli_js`（2026-09-08 修复） |
| 引擎 venv/模型 | 数据根 `venvs/.venv-*` / `models/`（受管，S12 就绪判定双位置） | 同左（`Scripts/python.exe` ↔ `bin/python3` 平台助手已有） | start-all.js / asr-server.js / lib.rs |
| 清理 | 全局清理 envs/all 档含 `app_data_dir/server`（与便携 node 同段） | 同左 | `lib.rs app_private_scope_targets` |
| 版本指纹 | `.version`=SERVER_VERSION（stage 写入）；升版重发 .app → 整目录重建 | 同左 | stage 脚本 + lib.rs |

> 结论：阶段 3 的核心逻辑没有平台分支依赖（纯 Rust/fs + 平台无关的 JS 模板），**预期在 Win 上同构可用**；阶段 4 只需按清单实测确认，而不是"再写一遍"。

### 6.7 阶段 4：Win 验证清单（映射 mac 已验项，000-temp-test.md 的 Win 版）

前置：Win 开发机已能构建（MSVC + `npm run build` → NSIS/MSI；WebView2 自带或安装器补）。

1. **单产物自包含**：设置页清空「服务代码目录」→ 运行构建产物 → 首启物化到 `%APPDATA%\world.opensound.local\server`（`.version`=SERVER_VERSION、含 start-all.js/engines/vendor，无 node_modules）→ **全程不引用仓库 asr-server**（进程 cwd/打开文件核查）。
2. **依赖自举**：引导条「安装 Node」→ npm ci 于物化目录成功（验证 `npm_cli_js` Windows 布局 + `.npmrc` allow-scripts 行为一致）→ 服务运行。
3. **功能**：kokoro 朗读 / sensevoice 量化识别 / qwen3（数据根受管 venv，N 卡 CUDA 与 CPU 分档各测一次）→ 卸载/全局清理复验（envs/all 档应清掉 `%APPDATA%…\server`）。
4. **版本刷新**：升 asr-server 版本 → 重打包 → `.version` 不符 → 物化目录整目录重建 → 重装依赖 → `/health` 新版本。
5. **发布专项（000-plan-14 待实测项）**：无 WebView2 老 Win10 的 Offline Installer/Fixed Version；VC++ v14 redist 是否必需（干净机实测 sherpa/node-llama 加载）；NSIS installMode/签名（见 000-plan-14 §2）；SmartScreen。

### 6.8 mac/win 已知差异与未决（发布前）

- Intel Mac 明确不支持（便携 Node 仅 darwin-arm64，001 §6.2 缺口 1 保留）；Windows ARM 不在范围。
- macOS 公证/Gatekeeper、Windows 签名 = 发布前提（000-plan-14 §2、000-plan-5 排期）；未签名期仅供本机自测。
- 系统识别/朗读的 WinRT（Win10 10240+）与 mac SFSpeechRecognizer/AVSpeechSynthesizer 能力差异见 000-plan-14 §3。

### 6.9 Win 阶段4 实测结论与 Win/mac 差异收敛（2026-09-09，详见 062/063/000-summary §7.9）

> 阶段 4 多轮实测把“跨平台形态里藏着的 Win 专属差异”逐个逼出来并已修，记录于此供 mac 回归对照。

| # | Win 实测差异 | 影响 | 修复与代码位置 | mac 是否受影响 |
|---|---|---|---|---|
| 1 | `fs::canonicalize(node)` 返回 `\\?\` verbatim 路径；node 24 当主模块参数崩 `EISDIR lstat 'D:'` | 点「安装 Node」0 秒失败 | `npm_cli_js`：Win 不用 canonicalize（node/npm 同目录直拼）；Unix 保留（解 bin/npm 符号链接） | 否（unix canonicalize 得正常路径） |
| 2 | uv 受管 CPython“别名目录”是 junction，管理员上下文创建后普通权限穿不过 → uv `os error 44` / venv trampoline `entity not found` | 一切 uv 操作、老 venv 启动失败 | 启动/「装 Python 基础」自动删 reparse 别名 + venv `pyvenv.cfg home` 改写真实目录（lib.rs） | mac 无此类 junction/信任问题，但归一化逻辑幂等无害 |
| 3 | 旧版 uv 建的 venv trampoline（45568B）无法配置自愈，只能 `uv venv --clear` 重建 | 8002（老 venv）起不来 | readiness + 安装器统一“关键包齐全 且 python 可 spawn”判定 | 老 venv 同样适用（mac 早期 venv 若为旧 uv 亦需重建） |
| 4 | vendored `Matcha-TTS/matcha/models` 缺口 + jsdelivr flat 清单前导 `/` | cosy 每次整目录重建后“缺环境” | 8 文件进模板 + filter 前剥前导斜杠（asr-server.js） | 两平台同修 |
| 5 | 安装锁滞留 409（客户端断连 + installer 不结束） | 连续安装需重启服务 | installLock 活动时间戳 + 看门狗 + 断开 20s 兜底（062 P1） | 两平台同修 |
| 6 | python 子进程 GBK 输出乱码、asr-server.log 混编 | UI/日志中文乱码 | `PYTHONUTF8`/`PYTHONIOENCODING` 注入 + 逐行 utf-8→gbk 回退（062 P2） | mac 默认 UTF-8，无此问题（注入无害） |
| 7 | 整目录重建丢 node_modules → 每次升级重装 node | 升级体验差 | package-lock 未变则保留 node_modules（063 ②） | 两平台同修（mac 升级同样免重装） |
| 8 | 卸载/重装心智 | — | 澄清：App 数据目录（含 server 代码）会被清/重建；自定义数据根（models/venvs/音色）持久 | mac 同构（app_data vs Library/Application Support） |

- **阶段 4 清单回填（§6.7）**：第 1 项自包含（物化/版本指纹）经 2.10.2→2.10.8 多轮升版验证 ✓；第 2 项自举（node/npm ci/Windows npm 布局）✓；第 3 项功能（kokoro/sensevoice 量化/qwen3/cosyvoice/8002）本机逐引擎验证中（8002 老 venv 已给重建入口，2.10.8 待最后确认）；第 4 项版本刷新现因“lock 未变保留 node_modules”成本≈0 ✓；第 5 项发布专项仍待（000-plan-14）。

## 七、变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-27 | 初版：盘点 14 处 macOS 专属依赖 + 四阶段适配方案 + 10 条注意事项 + 共性目标检查表 |
| 2026-08-27 | Win 实测更正：① sherpa-onnx-node@1.13.4 **自带全平台 optionalDependencies**（含 `sherpa-onnx-win-x64@^1.13.4`），Win 上 `npm ci` 自动装原生二进制，**无需**手动加 §阶段C 所述包（`sherpa-onnx-win32-x64` 在 npm 不存在，E404）；② `engineReadiness` 的 runtime 检查已跨平台化（`bin/python3` ↔ `Scripts/python.exe`，见 asr-server.js `runtimePathExists`）；③ 032 已在 App 内实现"一键运行时自举"（引导条 + 便携 node + npm ci），解决 §五检查表首行。 |
| 2026-08-30 | 追加 §五·补：macOS 从零验证提醒（000-plan-2 阶段 E）——Gatekeeper/公证、MPS、venv 路径、lsof、模型源、uv 自举、统一内存评估、数据目录、从零验收清单 10 项；提醒 mac 未验证项=整个从零流程，发布前必须在干净 mac 走一遍 |
| 2026-09-05 | **Win 已完成后的现状结论 + Mac 第二阶段对齐（新增 §六，代码只读审计）**：① 结论 A（Win→其它 Windows 电脑）：App 内自举闭环已在本机「卸载→从零」验证，换陌生机器前提 = x64 Win10/11 + 联网 + 按引擎预留空间；无 N 卡自动 CPU、有 N 卡 GPU/CPU 自选；唯一保留 = 干净第二台 PC 的真机从零验收（D3）未做。② 结论 B（Mac）：跨平台自举代码（ensure_node darwin-arm64 / uv 双架构 / uv 受管 CPython 3.11 / venvPy 平台助手 / auto cuda→mps→cpu / HAS_NVIDIA=false 禁 CUDA）审计通过；**缺口登记：ensure_node 缺 darwin-x64（Intel）、node-llama-cpp Metal 需 macOS 15+、MPS 系引擎改动后未真机回归、公证/Gatekeeper**。③ 普通新 Mac 用户可全 App 内完成（不依赖系统 python/node，版本由 uv 受管 3.11 保证一致）。④ 第二阶段建议 P1 代码补齐（Intel node 分支 + 平台分支清点）/ P2 = 000-plan-2 阶段 E 从零验收（逐引擎清单）/ P3 打磨（mlx-audio 通道、Intel 低优先）。 |
| 2026-09-08 | 新增 §六·补「产品主线阶段视图与阶段 4 指引」：定义阶段 1 mac 开发 / 2 Win 开发验证 / 3（当前）功能修改 + S6′ 单产物自包含（mac 已实测通过）/ 4 Win 验证；说明 S6′ 逻辑的平台无关形态（内置模板、物化目录、npm_cli_js 多布局、受管 venv/模型、清理档、.version 指纹整目录重建）；给出阶段 4 Win 验证清单（自包含/自举/功能/版本刷新/发布专项）与未决差异。README 同步改为单产物口径。 |
| 2026-09-09 | 新增 §6.9：阶段 4 Win 实测差异收敛表（canonicalize→verbatim EISDIR / junction untrusted os error 44 / 旧 uv venv trampoline 需重建 / matcha 模板补全 / 安装锁自愈 / GBK 乱码 / node_modules 保留 / 数据根心智），附 mac 是否受影响列；回填 §6.7 阶段4 清单状态。详录见 062 / 063 / 000-summary §7.9。 |
