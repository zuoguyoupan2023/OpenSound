# 034 · 后续规划：先跑通模型 → 再落地「用户自主动作」的分发形态

> 目标一句话：**先把模型在 Win 上跑通（当前阶段），再把"下载 App → 自己点击安装/下载 → 能用"做成普通用户在 Win/Mac 都开箱即用的最终形态。**
> 铁律贯穿始终：**一切安装/下载由用户自己点击触发，App 只检测 + 提示，绝不自动执行**（另见 `000-交付铁律-一切用户自主动作.md`）。

---

## 阶段 0（当前 · 进行中）：模型下载 + Win 跑通

- 目标：SenseVoice（识别）+ Kokoro（朗读）+ LLM（对话）在 Win 全部可用，**python 系引擎（Qwen3-TTS / SenseVoice 原始版 / CosyVoice 克隆）模型文件先下好、环境后续解锁**。
- 交付物：
  - `download-all-models.ps1`：一键下载全部模型到数据目录（分文件夹、断点续传、字节校验）——**用户自己在终端跑（本阶段开发者实操；最终形态转为 App 内操作）**。
  - 验收：识别/朗读/对话三链路在 Win 出声、出字。
- 文档：`033`（Win 踩坑）、`001`（跨平台适配更新）。

## 阶段 1：App 内「环境检测 + 引导」（把"自己点击"做成功能）

- App 启动/设置页做**运行时检测**（`check_runtime` 已有）：
  - node：版本 ≥18（PATH / 常见位置 / 数据目录便携版）；
  - python：≥3.10（PATH / 受管）；
  - 服务依赖（node_modules / venv）就绪度。
- 缺失时：**只提示**（引导条 / 设置页），给按钮，**用户点了才装**（`install_runtime` 已有但启动不再自动触发——P2 已拍板）。
- 所有安装动作：进度可见、可取消、失败可重试；镜像可切（npmmirror / modelscope）。
- 验收：干净机器打开 App → 界面明确提示缺什么 → 用户点按钮逐步完成，全程零终端。

## 阶段 2：分发形态二选一（用户拍板后定）

### 策略 A · 纯壳 + 用户自备/自装运行时（小而纯）
- App = 纯壳（~几 MB）；node/py **不内置**。
- App 检测：已有 → 直接用；
- 缺失 → App 内指引 + 按钮：
  - node：弹出官方/镜像下载页（用户自己装），App 检测到后继续；
  - python：同样指引 + 后续 uv 自举（见下）；
- 模型：用户在模型页自己点下载。
- 优点：包小、与用户环境互不干扰；缺点：多一步用户自装。

### 策略 B · 运行时全内置（大一点，拿来即用）
- App 打包便携运行时（数据目录 `runtime/` 内自管，**不动系统 PATH，与用户已有安装零冲突**）：
  - node：便携版（zip 解压到 `runtime/node`，服务用受管 node 启动）；
  - python：uv 自举（`runtime/uv` + `uv python install` 独立 CPython → `runtime/venvs/*`）；
- 用户只做：打开 App → 模型页自己点下载 → 即用。
- 优点：开箱即用（目标形态）；缺点：包体增大（估计 +100~150MB）。

> **默认建议**：策略 B（贴合"普通用户拿来就用"目标）；若体积敏感，退化为 A。二者**共用同一套检测/引导骨架**，差异只在"运行时从哪来"。

## 阶段 3：python 系引擎全解锁（uv 自举）

- App 内下载 `uv`（单二进制，跨平台）→ `uv python install 3.11`（独立 CPython，无需系统 python）→ 按引擎 requirements-lock 建 venv + 装依赖（torch 等）→ **服务启动时模型自动拉取**（sensevoice-original / qwen3 / cosyvoice 已提前下好的文件直接取用）。
- 修 P3 遗留：`sensevoice-server.py` 等模型路径改为从数据目录读取（033 §8/344 相关）。
- 验收：Win/Mac 上 python 系引擎全部点亮（识别增强 / 高音质朗读 / 音色克隆）。

## 阶段 4：打包与交付闭合（P4）

- asr-server 服务端代码（源码 + engines + vendor + locks）打进 App resources（`bundle.resources`），启动优先用包内资源；安装包开箱即含全部服务端脚本（见用户对"脚本应在 App 里"的要求）。
- 发布双产物：Win（NSIS/MSI）+ Mac（dmg）；README 三步上手。
- 验收（普通用户视角）：**下载 App → 自己点击安装/下载 → 能跑**；Win/Mac 一致。

---

## 铁律复述（每阶段验收都检查这条）

1. **不自动执行**：任何 node/py/依赖/模型的安装与下载，只能由用户点击触发；App 只检测与提示。
2. **用户授权可见**：每一步都有进度、可取消、失败可重试。
3. **双平台**：Win + Mac 同一条路径。

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-27 | 初版：阶段 0（Win 跑通）→ 阶段 1（检测+引导）→ 阶段 2（A/B 分发）→ 阶段 3（uv 自举解锁 python 系）→ 阶段 4（打包闭合）。 |