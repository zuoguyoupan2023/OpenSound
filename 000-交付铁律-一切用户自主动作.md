# 000 · 交付铁律 —— 最终成品的一切都由用户自主动作

> **这篇文档是项目的"出门准则"。写在这里，读到的人（无论人还是 AI）都必须先记住它。**
> 它压过一切功能、规划、优先级。任何开发、评审、接手新会话，先重读这一篇。

## ★ 最终成品的样子（唯一的验收标准）

> **普通用户下载 App → 打开 → 看到清楚的状态 → 自己点击安装 / 自己点击下载 → 能用。**
> 全程零终端、零"偷偷发生的事"。这是最终形态，铁打不动的验收标准。

## ★ 三条铁律（违反 = 返工）

### 铁律一：绝！对！不自动执行！
- App **只检测 + 提示**（缺 node？缺 python？缺依赖？缺模型？→ 界面明明白白告诉你）。
- **任何安装、下载、更新，必须由用户自己点击触发**。
- 禁止：启动即安装、静默下载、后台拉起依赖、偷偷装东西。
- AI/开发者在本项目里的同类红线：**不要替我执行用户的安装动作**（033 §15 教训：连"杀残留进程"都要交给用户或明确 PID）。

### 铁律二：用户授权必须可见、可控
- 每步安装/下载：**有进度、可取消、失败可重试、镜像可切换**。
- 大流量下载（>1GB）要有二次确认。
- 用户点了才开始 → 点了取消就停。

### 铁律三：Win 与 Mac 同一条路径
- 最终成品在 **Windows 与 macOS 都能开箱即用**（同一套 UI、同一套"用户点击"流程）。
- 平台差异只收敛到实现层（033 记录的所有 Win 坑都是为此服务的），用户侧体验一致。

## 为什么反复强调（背景）

- 本项目目标是"其他人拿到就能用"：目标用户**不会开终端、不想折腾环境**。
- 曾经的设计偏差：想让 App 启动时"自动装好一切" → 用户明确否决：**不要自动执行，用户自己点**（034 文档已把这条写进每个阶段）。
- 常见反例（都是返工）：启动自动 npm ci、静默下载模型、自动修文件、开发者后台替用户装依赖。

## 每个阶段验收前，先过这三问

1. 用户打开 App，缺什么是不是**看得到**？（检测可见）
2. 是不是**只能由用户点击**才发生安装/下载？（无自动）
3. Windows / macOS 是不是都走同一条"用户点击"路径？✈️

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-27 | 初版：交付铁律——用户自主动作、授权可见、双平台一致。 |