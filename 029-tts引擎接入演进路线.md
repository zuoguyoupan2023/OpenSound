# 029 · TTS 引擎接入演进路线（插件 → adapter → fork 自研）

> 回答一个问题：**系统朗读能力怎么接进 OpenSound？先插件还是先自建？什么时候 fork？迁移成本怎么锁死？**
> 结论先行：**先用社区插件 + 自建一层 adapter 锁死接口；出现插件覆盖不了的需求时按触发条件升级到 fork 或自建 command 与插件并存；通用改动回馈上游。**
> 关联文档：`028-mac-win系统TTS与Tauri调用调研.md` §3（五条路线对比）、§8（复刻对比）、`009`（云端引擎注册表——同一套思路在云端 API 上的应用）
> 口径：2026-08-26 · 插件现状：`tauri-plugin-tts` v0.1.13（MIT）

---

## 一、为什么不是"直接自建"或"永远用插件"

| 方案 | 问题 |
| --- | --- |
| 一开始就纯自建 | 插件已解决最脏的活：跨平台 rate/pitch/volume 刻度归一化、事件统一分发（`tts://speech:*`）、capability 权限对接。自己重写至少数天且要双平台验证 |
| 永远依赖插件 | 个人项目、版本 0.1.x；不支持音频导出落盘（011 朗读历史刚需）、桌面端不能暂停、拿不到 Personal Voice/SAPI5 老声 |

所以正确姿势是：**用它的能力，但不让它的接口渗进业务代码**。

## 二、三步演进路线

```
第 1 步 · 插件 + adapter（现在就做，半天）
─────────────────────────────────────────────
前端唯一入口 = src/ui/services/sysTts.ts（自己写）
  - 内部转发 tauri-plugin-tts-api 的 speak/stop/getVoices/onSpeechEvent
  - 对外暴露自己的稳定接口（见 §三）
  - 业务组件只 import sysTts，绝不直接 import 插件包
效果：插件换掉时只改一个文件

第 2 步 · 触发式升级（出现下表任一需求才动手）
─────────────────────────────────────────────
优先顺序：并存自建 command > 硬 fork 插件
  - "并存"：插件继续管播放；另写自己的 #[tauri::command] 管
    新能力（如 macOS `say -o` 导出落盘），两者互不阻塞
  - "硬 fork"仅在需要改插件内部行为（如解锁 tolk/SAPI 后端）时才值得

第 3 步 · 回馈上游（随手做）
─────────────────────────────────────────────
通用改动提 PR 回 brenogonzaga/tauri-plugin-tts（MIT 无障碍），
减少长期分叉维护成本
```

### 第 2 步的触发条件清单（满足任一即升级）

| # | 触发条件 | 对应路线 | 预估工作量 |
| --- | --- | --- | --- |
| 1 | 朗读历史要把合成结果**导出成音频文件**（011 方案） | 并存自建：mac 用 `say -o` / AVSpeechSynthesizer.write 写 command；Windows 待专项调研 | mac 半天～1 天 |
| 2 | 需要**暂停/恢复**（桌面端插件仅 iOS 生效） | fork（改 AVSpeechSynthesizer 调用点）或路线 E | 2–3 天 |
| 3 | 要 Windows **SAPI5 老声音**或 Personal Voice | fork（开 tts crate `tolk` feature / 补专属 entitlement 流程） | 3–5 天 |
| 4 | 插件弃坑/长期不兼容新版 Tauri | 整体替换为路线 C（`tts` crate 自写 command），adapter 保证业务零改动 | 2–4 天 |

## 三、adapter 接口草案（第 1 步交付物）

```typescript
// src/ui/services/sysTts.ts —— 业务层唯一可见的系统朗读接口
export interface SysVoice { id: string; name: string; lang: string }
export interface SpeakOptions {
  text: string;            // 调用方不用关心 10KB 字节上限，adapter 内自动分段
  voiceId?: string;
  rate?: number; pitch?: number; volume?: number;
  queue?: 'flush' | 'add';
}
export interface SysTtsAdapter {
  isAvailable(): Promise<boolean>;
  listVoices(langPrefix?: string): Promise<SysVoice[]>;
  speak(opts: SpeakOptions): Promise<void>;          // 开始即 resolve
  onStop(cb: () => void): () => void;                // 封装 speech:finish/cancel
  // v2 预留（第 2 步实现，接口先行占位，避免未来破坏性变更）：
  synthesizeToFile?(opts: SpeakOptions & { path: string }): Promise<string>;
}
```

要点：分段逻辑（10KB 字节限制）、voiceURI 持久化校验、事件转 promise 都收在 adapter 内——这正是 000-device-tts §七 Checklist 在代码里的落点。

## 四、风险与纪律

1. 版本锁定 `tauri-plugin-tts = "=0.1.13"`，升级走显式变更；
2. CI 双平台构建必须包含插件（它带原生依赖）；
3. adapter 是唯一允许 import `tauri-plugin-tts-api` 的文件——加一条 lint 规则防手滑；
4. 云端 TTS 引擎（009 的 `TTS_ENGINES` 注册表）与系统朗读 adapter 平级并列，UI 上是两个独立选项，不混用状态。

## 五、变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-26 | 初版：三步演进路线 + 触发条件清单 + adapter 接口草案 + 纪律条款（自 028 §4 展开成文） |
