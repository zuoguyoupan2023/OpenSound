# 032 · 运行时自举与数据目录统一设计（App 内一键安装，普通用户免终端）

> 回答两个问题：① 普通用户拿到 App，缺 node/Python/依赖时**在 App 界面里一键搞定**（不碰终端）；
> ② **模型/缓存/环境放哪儿**——不写死在 `asr-server/` 代码目录，而是收敛到独立的用户数据目录。
> 状态：W1（node 自举 + 一键安装 UI）已实现并构建验证；本文为整体设计与 W2+ 依据。
> 关联：`002-plan`（自举与分发）、`031`（Win/mac 适配）、`010`（环境可靠性）。

---

## 一、目标（用户视角）

```
下载 App → 打开 → 若缺运行环境，顶部出现引导条 [一键安装]
→ App 自动下载便携版 Node / 安装依赖 / 拉起服务
→ 模型页显示 8 个引擎真实状态 → 点「补齐」下载模型 → 识别/朗读/对话/克隆全部可用
全程无终端、无需懂环境；模型与数据在用户数据目录（不占代码目录）。
```

## 二、三层架构（职责分离）

| 层 | 内容 | 位置 | 可写 |
|---|---|---|---|
| L1 App 壳 | UI + 编排 + 运行时自举（本设计） | Tauri bundle | 否 |
| L2 服务代码 | asr-server.js / *.py / vendor/ / engines/*.json / locks | 随包分发（或设置指定目录） | 否（只读） |
| L3 用户数据 | **模型 / 运行时(node,uv,python) / venv / 缓存 / 日志 / 音色** | 用户数据目录 | ✅ 是 |

关键原则：**代码目录只读，数据全在 L3**。更新 = 换 L2，用户数据不丢。

## 三、数据目录（L3）布局

默认 = Tauri `app_data_dir`，可在设置里自定义（换盘/迁移）：
- Windows：`%APPDATA%\world.opensound.local\`
- macOS：`~/Library/Application Support/world.opensound.local/`

```
data_dir/
├─ config.json            # 现有配置（含 server_path / ui 设置 / token）
├─ runtime/               # App 自举的运行时（免系统安装）
│  ├─ node/               # 便携版 node（node-vXX-win-x64/node.exe）
│  ├─ uv/                 # uv（python 自举工具，单二进制）
│  └─ python/             # uv python install 托管的独立 CPython
├─ models/                # 全部模型权重（替代 asr-server/models）
│  ├─ sensevoice/  tts/  llm/  hf/  modelscope/  cosyvoice/
├─ venvs/                 # .venv-qwen3 / .venv-cosyvoice / .venv-funasr
├─ cache/                 # numba / mpl（原 asr-server/data/cache）
├─ voices/                # 克隆音色样本（原 asr-server/data/clone-voices）
└─ logs/asr-server.log
```

服务代码启动时注入 `OPENSOUND_DATA_DIR=<data_dir>`，**所有落盘路径由它派生**
（asr-server.js 的 CACHE_DIR、start-all.js 的 MODELS_DIR/HF_HOME/MODELSCOPE_CACHE、
各 py 服务缓存、Rust 侧日志）——把现有 `path.join(__dirname,...)` 全部改为从 env 派生，纯路径层改造，不动业务逻辑。

**兼容迁移**（老用户）：数据目录为空且旧 `asr-server/models` 有内容时，首次启动提示
「复用旧模型目录 / 迁移」二选一；迁移用同盘 rename（快），否则复制。设置页可随时改数据目录。

## 四、运行时自举（App 内一键）

| 依赖 | 检测 | 缺失时 App 内动作 |
|---|---|---|
| Node.js ≥18 | `check_runtime`（PATH/常见位置/受管便携版 + 版本） | 下载**便携版** node zip（nodejs.org → npmmirror 镜像，带进度条）解压到 `runtime/node/`，免安装 |
| 服务端依赖 | `asr-server/node_modules` 关键包存在性 | App 内 `npm ci`（npmmirror registry，行级进度事件）；npm≥11 自动批准原生二进制 scripts |
| Python ≥3.10 | `python --version`（仅展示） | 用 **uv**：下载单二进制 `uv.exe` → `uv python install 3.11`（独立 CPython）→ `uv venv` + `uv pip install -r <lock>` 建各引擎 venv（W2） |
| 模型 | engines/*.json 逐文件校验 | 已有模型页「补齐」按钮（W1 已通） |

自举全程 UI 可见：主界面顶部引导条（缺什么、一键安装、进度条、日志行、失败重试）+ 设置页「环境与运行时」区块（状态明细 + 一键安装/修复）。
App 启动时自动预检：不完整 → 自动触发自举（普通用户零操作）。

## 五、实施阶段

- **W1 ✅（本阶段）**：node/依赖自举 + 引导条 + 设置区块 + 启动自动预检；App 构建验证。
- **W2**：python 自举（uv）→ 各引擎 venv 自动创建；数据目录分离（OPENSOUND_DATA_DIR 全量派生）+ 迁移向导。
- **W3**：打包形态（L2 服务代码进 bundle resources 或随 zip；发布体积红线 002 §S6）。

## 六、待拍板

1. python 自举：uv（推荐，无需系统 python、可 pin 版本、镜像可切）vs embeddable zip —— 建议 uv。
2. 数据目录默认 app_data_dir，是否开放「首次启动自选目录」？（建议：默认即可，设置可改）
3. 老模型迁移：复用/迁移二选一（建议默认「复用」零拷贝）。
4. L2 服务代码随包（更新需重发 App）vs 独立 zip 指认目录（可独立更新）——建议先随包，保持「拿来就用」。

## 七、变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-27 | W1 实现：Rust `check_runtime`/`install_runtime`（便携 node 下载解压 + npm ci 进度 + 自动拉起服务）、前端引导条 + 设置页运行时区块、启动自动预检；TS/cargo 编译通过。 |