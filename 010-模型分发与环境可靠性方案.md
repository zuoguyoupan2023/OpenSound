# 010 · 模型分发与环境可靠性方案（含系列问题解答）

> 目的：回答「打包 App（16.5MB）分发给其他人后，模型库点"安装"到底装了什么、装到哪、还缺什么」，并给出 CosyVoice 源码依赖的根治方案与整体分发策略。**本文档只是方案，未实施**；后续将开新对话执行。
> 衔接：`005-cosyvoice模型下载.md`（手动下载 9GB）、`000-summary.md §六`（克隆实现说明）、`006-voice-后续规划.md`（"不要把大模型打进安装包"原则）、`007-product-UI-models规划.md`（模型清单 UI）。
> 背景：2026-08-22 排查发现克隆服务不可用的根因是 `asr-server/CosyVoice/` 源码目录丢失（见 000 §7.6 补充）；已在 `asr-server/asr-server.js` 增加 `cosyvoice-clone` 安装器（检查模型 + 缺源码时自动浅克隆）。

---

## 一、问题清单（原话整理）

1. 设置里的「模型保存路径」指向 `<项目>/asr-server`，其他人在自己电脑上点"安装"，识别 / LLM / 朗读的模型都会放到对应位置吗？
2. CosyVoice 的源码问题，也会被自动克隆并放进对应位置吗？
3. 要不要干脆把代码集成进 App？
4. 其他模型好像都是"模型 + 必要文件"，不需要源码——这是 CosyVoice 技术路线的原因，还是官方偷懒？有没有**不损失模型质量**、又像其他模型那样可靠（只下载模型+必要文件）的方式？
5. 其他模型有类似问题吗？`000-summary` 里列出的模型都会出现类似问题吗？

## 二、现状核实（以代码为准，2026-08-22）

### 2.1 「模型保存路径」的真实含义

- 设置项存的是 **asr-server 后端目录**（`get_server_path` / `set_server_path`，Tauri 侧校验该目录下是否存在 `start-all.js`，见 `src-tauri/src/lib.rs`），并非单纯的"模型目录"。
- 所有安装器的落盘根都是 `CACHE_DIR = <asr-server目录>/models`（`asr-server.js` L33）。所以：**换一台机器，只要把 asr-server 目录整体搬过去并在设置里指认，模型就会装进它的 `models/` 子目录** —— 问题 1 的答案是"基本对，但有例外"，见 2.2。

### 2.2 各引擎"点安装"行为与真实落盘位置

| 引擎 | 运行时依赖 | 权重来哪 | 实际落盘位置 | 点「安装」做什么 |
|---|---|---|---|---|
| Kokoro | sherpa-onnx-node（npm，随 node_modules） | 安装器下载 | `models/tts/kokoro-multi-lang-v1_0/` ✓ | 下载 ONNX 权重 |
| SenseVoice（量化版） | sherpa-onnx-node | 安装器下载 | `models/sensevoice/model.int8.onnx` 等 ✓ | 下载 ONNX 权重 |
| LLM（llama-cpp） | node-llama-cpp prebuilt 二进制 | 安装器下载 | `models/llm/*.gguf`（`.part` 完成后原子改名）✓ | curl 下载 GGUF |
| Whisper | transformers.js（npm） | 首次识别自动下 | **`asr-server/.cache/`**（transformers.js 默认 `env.cacheDir='./.cache'`，随启动 cwd）⚠️ 不在 models 下 | 仅提示"首次使用自动下载" |
| Qwen3-TTS | `.venv-qwen3`（python + torch + transformers） | 服务首启自动下 | **`~/.cache/huggingface/hub/`** ⚠️ 不在 models 下 | 仅提示"首启自动下载" |
| SenseVoice 原始版 | **系统 python**（硬编码 `/opt/homebrew/bin/python3`）+ pip funasr | funasr 运行时自下 | `~/.cache`（modelscope/funasr 默认）⚠️ | 无安装器（随后台服务） |
| CosyVoice3 克隆 | `.venv-cosyvoice` + **CosyVoice 源码目录** | 9GB 手动预下载（005） | `models/cosyvoice/Fun-CosyVoice3-0.5B/` ✓ + `CosyVoice/` 源码 ✓ | 检查模型完整性；缺源码则自动 `git clone --depth 1 --recursive` |

**结论：**
- 问题 1：kokoro / sensevoice / llm / cosyvoice(模型部分) ✓ 会落到 `models/` 对应子目录；**whisper 与 qwen3 不会**（分别落在 `./.cache` 与 `~/.cache/huggingface`）——这是位置不统一的历史遗留，应修（见 §五 行动项）。
- 问题 2：✓ 会。新安装器会在缺源码时自动浅克隆（几十 MB）放进 `<asr-server>/CosyVoice/`。**前提**：目标机器装有 `git` 且能访问 github.com。

### 2.3 新用户拿到 16.5MB App 后的真实差距（比"模型在哪"更大）

App 只是 Tauri 壳。要跑起来还需要这些**不会被"安装"按钮创建**的东西：

| 依赖 | 本机现状 | 新用户机器 |
|---|---|---|
| Node.js | `find_node()` 找 PATH/nvm/homebrew | 大概率没有 → 服务起不来 |
| `asr-server/node_modules` | npm install 过 | 没有 → asr-server 无法启动 |
| `.venv-qwen3` / `.venv-cosyvoice` | 手工建好 | 没有 → qwen3/cosyvoice 起不来 |
| 系统 python + funasr | `/opt/homebrew/bin/python3` 硬编码 | macOS 无 homebrew python 则 8002 起不来 |
| 9GB CosyVoice 权重 | 已在 models/ | 需按 005 手动下载（安装器只检查不代下） |

**因此当前形态实际是"开发者本机可用"；对外分发需要一次专门的打包/引导工程**（§四.3）。

## 三、CosyVoice 为什么需要源码（问题 3/4 解答）

### 3.1 技术路线对比：三类引擎的分发形态

| 形态 | 引擎 | 组成 | 为什么不需要源码 |
|---|---|---|---|
| C++ 运行时 + ONNX 权重 | kokoro、sensevoice | sherpa-onnx-node + 若干 .onnx | 推理器是通用二进制，权重即全部 |
| 标准 Python 生态 | qwen3-tts、funasr、whisper(py) | pip 包 + HF/ModelScope 权重 | 官方发了 pip 包 / 进了 transformers 生态 |
| **研究型源码仓库** | **CosyVoice** | git 仓库 + third_party 子模块 + 权重 | **官方未发布 pip 包**；推理代码（flow/hift/llm 封装）与 `third_party/Matcha-TTS` 强耦合，README 标准用法就是 `git clone --recursive` |

所以**不是偷懒，是生态成熟度差异**：CosyVoice3（2025.12 发布）尚处于"研究仓库"阶段，推理栈没进标准分发渠道。调研报告（`2026-实时语音与语音克隆开源方案调研报告.md` §2.1）也显示其竞品 DOTS-TTS 已支持 PyPI 安装——未来 CosyVoice 官方若出 pip 包，可无缝切换。

### 3.2 有没有"只下模型+必要文件"且零质量损失的方案？

有。质量由**权重 + 推理算法**决定，把推理代码从"git clone"改为"固定版本的本地文件"不影响任何数值结果：

- **方案 A（推荐）· 源码 vendoring**：把锁定 commit 的 CosyVoice 必需子集（`cosyvoice/` 包 + `third_party/Matcha-TTS` 中被 import 的部分，浅克隆实测仅 ~7MB）直接纳入 asr-server 目录（git 管理）。Apache-2.0 许可允许，保留 LICENSE 与版本号。离线、可复现、不再怕目录丢失；现有 installer 里的 git clone 逻辑降级为兜底。
- **方案 B · 本地 wheel/快照**：一次性 `pip wheel` 或打包 site-packages 子集，安装脚本把它装进 `.venv-cosyvoice`。本质同 A，多一步构建，收益不大。
- **方案 C · ONNX/CoreML 化接入 sherpa-onnx**：真正做成"纯模型文件"，但导出链路复杂、零样本克隆管线易损，**有质量风险，不符合"零损失"约束**，仅作远期观察项（若官方或 sherpa 生态出了 CosyVoice3 ONNX 支持再评估）。

### 3.3 要不要把代码集成进 App 壳？

**不建议**。16.5MB 壳保持纯粹（UI + 编排），理由：
- 代码/环境/模型的更新节奏完全不同，塞进壳意味着每次改后端都要重发 App；
- `006` 已确立"大模型不打进安装包"原则，同理适用于运行时资产；
- 正确分层是：**App 壳（不变）+ asr-server 目录（代码/venv/模型，可独立更新、可指路径）**。vendoring（§3.2-A）正是把"源码"变成 asr-server 目录内的普通受管文件，而非塞进 App。

## 四、其他模型会有类似问题吗（问题 5）

| 模型 | 源码依赖类问题 | 环境依赖类问题 |
|---|---|---|
| kokoro / sensevoice | 无 | 仅需 node_modules |
| llm (llama.cpp) | 无（prebuilt 二进制） | 仅需 node_modules |
| whisper | 无 | 同上；另缓存位置不规范（§2.2） |
| qwen3 | 无（transformers 生态） | 需要 `.venv-qwen3`；缓存在 `~/.cache` |
| sensevoice-original | 无（funasr 有 pip） | **最重**：系统 python 硬编码 + pip funasr |
| cosyvoice | **有（唯一）** → §3.2 方案 A 根治 | 需要 `.venv-cosyvoice` + git（兜底克隆用） |

结论：**"源码缺失导致服务崩溃"只有 CosyVoice 一家**（已通过安装器+vendoring 方案双保险）；但**所有 Python 系引擎共享"运行环境不会被自动创建"这一更大的分发短板**（§2.3），这才是对外分发的真正工作量所在。

## 五、行动项清单（供后续对话执行，按优先级）

1. 【高】vendoring CosyVoice 源码子集进 asr-server（§3.2-A），installer 改为"校验优先、clone 兜底"。
2. 【中】统一模型缓存位置：给 qwen3 设 `HF_HOME=<asr-server>/models/hf`（目录已存在 `models/hf`！）、给 whisper 设 `TRANSFORMERS_CACHE=<asr-server>/models/whisper`（start-all.js 注入即可，不动各 server 代码）。
3. 【中】去掉 `/opt/homebrew/bin/python3` 硬编码：改为环境变量/探测序列（`PY_SYS` env 已支持，补默认值探测 `python3` ∈ PATH）。
4. 【低】对外分发专项（独立立项）：Node sidecar 打包或首启引导器（检测并提示安装 node/venv/依赖），本文只立框架不做设计。

## 六、决策点（新对话开始前请拍板）

- [ ] 方案 A 的 vendoring 粒度：整仓浅拷贝 vs 裁剪子集（裁剪更小但升级麻烦）？
- [ ] `models/hf` 已存在于磁盘但代码未用 —— 是否确认把 HF_HOME 指过去（存量 ~/.cache 的 qwen3 权重需要迁移或重新下载）？
- [ ] 分发专项（§五.4）何时立项、目标平台范围（仅 macOS Apple Silicon 还是含 Intel/Windows）？
