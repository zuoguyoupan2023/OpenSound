# 050 · 本地部署规划：OmniVoice（0.8B，646 语，Apache-2.0）

> 目的：OmniVoice **独立本地验证/部署**（先源/路径/出声，最后回 App）。纪律见 043 §一；总清单见 043 §〇 #12。
> 关联：流程与踩坑口径复用 **047**（FireRedTTS3 已实测）。

---

## 一、这是什么 / 效果依据

| 项 | 内容 |
|---|---|
| 出品 | 开源社区（Apache-2.0，646 语种覆盖） |
| 特点 | 0.8B 轻量，646 语 TTS；有 [社区 GGUF 版](https://huggingface.co/Serveurperso/OmniVoice-GGUF) 可 CPU/量化 |
| 定位 | 语种覆盖广是卖点；单语言质量（尤其中文）需实测，可能不如专精模型 |
| 许可 | Apache-2.0（登记于 043） |

## 二、独立部署步骤（顺序执行，每步停下汇报）

1. **源实测**：官方仓库/模型卡 README raw（安装命令原文）；权重整仓字节清单（HF/ModelScope tree API）；GGUF 版是否官方发布（否→仅备选）。
2. **独立环境**：目录（如 `E:\Github\omnivoice\`）；venv + CUDA torch cu128（aliyun 平铺镜像）；PyPI aliyun 镜像；**预装 soundfile**（047 坑4）。
3. **模型**：整仓全量 + 字节校验；辅助件预下载（036 坑 F）。
4. **验证**：import 探针 + 中文/英文各一段出声（646 语重点测中文质量）。
5. **验收**：出声 wav + torch/cuda、模型体积、耗时/RTF、中文质量评价、许可登记。
6. **坑映射（047 全套）**：urllib 网络（坑1）；磁盘余量（坑2）；flash_attn grep（坑3）；soundfile（坑4）；分进程（坑5）；签名 bug（坑6）；UTF-8 无 BOM（坑7）。

## 三、风险/待实测登记
- ⚠️ tokenizer 可能大（646 语）；GGUF 非官方慎用；多语质量参差，中文必须实测；显存 0.8B 预计 <3GB fp16。

## 变更记录
| 日期 | 变更 |
|---|---|
| 2026-08-30 | 初版：OmniVoice 独立部署规划（来源：043 #12 + 047 流程复用） |