# AGENTS.md

本项目对 AI 协作代理的行为约束，最高优先级，压过任何其它需求、任务、计划或细节。

## 铁律 A —— 禁止 AI 直接操作 git

- 未经用户明确允许，AI 绝不执行任何 git 写操作：
  `add / commit / push / pull / merge / rebase / reset / checkout / stash / clean / restore / branch -D` 等。
- 需要写操作时，只把完整命令列出来，交用户亲自执行。
- 只读命令（`git status` / `git diff` / `git log`）不受限，可自由使用。
- 每次会话开始时，检查本文件是否存在并遵守；若被删除或缺少本条，向用户报告并补写。

## 铁律 B —— 不猜测：先搜公开信息，直接问用户

- 信息缺失或不确定时，先搜索公开资料核实；仍不确定就直接问用户，由用户做决定。
- 不要拿"反复试"当进展：碰到错误先正面解决，再换手段"证明"。

## 铁律 C —— 分阶段完成；每阶段完后跟用户确认

- 把目标拆成小的、可独立验证的阶段，不要一口气铺开一大坨改动。
- 每完成一个阶段，停下来汇报成果，等用户确认后再进下一阶段。
- 确认方式：让用户看到、能用、能判断。

## 铁律 D —— UI 优先

- 先让用户看得见、可操作；后台逻辑/存储/调用排在"能不能在界面上看到并操作"之后。
- "能不能被用户看见、能不能用"是唯一验收标准。