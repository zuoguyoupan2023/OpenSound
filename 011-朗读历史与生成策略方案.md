# 011 · 朗读历史、音频库角标、对话历史与数据存储规范（方案）

> 目的：① 评审原「朗读历史与生成策略」方案并修正与代码不符之处；② 音频库条目增加来源角标（识别 / 朗读 / 对话…）；③ 对话面板增加历史记录持久化；④ 给出全部用户数据的**存储位置总览与规范**。**本文档只是方案，未实施；确认后按 §七 阶段执行。**
> 衔接：`003-audio-storage规划.md`（音频库已实现）、`SPEC.md §3.4`（/speak 帧协议）、`000-summary.md`（面板结构）。
> 本版变更（v2）：补入代码核查发现的 **3 个新缺口**（停止语义失真、保存时机绑死播放、重复入库）、来源角标方案、对话历史方案、存储规范章节；原三方案对比结论**维持不变**。

---

## 一、需求（原话整理）

1. 评审 011 原方案的朗读优化逻辑是否合理，有遗漏/欠缺就提方案供选择；
2. 朗读内容进音频库时要有「朗读」角标；现在看起来都是「识别」界面来的，也要加「识别」角标；
3. 对话界面没有历史记录，希望有合理的保存逻辑；
4. 所有内容具体存在哪里？要有一套规范的存储逻辑，方便后续管理；
5. **先改文档、明确方案，确认后再执行。**

## 二、现状核实（逐行以代码为准）

### 2.1 朗读链路（已存在）

```
speakStream() ─ 帧流（每帧=4字节长度+一段16kHz WAV，服务端逐句推送）
   ├─ playStream → createFramePlayer 边收边播          （实时收听）
   └─ tee() → collectFrames 收集全部帧
        └─ 播放结束后 mergeWavFrames(帧[]) → 单个 WAV
             └─ saveTts(wav, engine, text) → Rust audio_save
                  └─ app_data_dir/audio/<id>.wav + index.json（kind=tts）
```

音频库面板已有「我的录音 / 朗读历史」两页签 + 播放 + 删除 + 导出（`AudioLibraryPanel.tsx`）。真实数据核实：本机已有 `recordings/` 19 条、`tts/` 7 条。

### 2.2 音频到底从哪些界面来（来源矩阵）

| 来源面板 | 存为 kind | engine 实参 | 内容 |
|---|---|---|---|
| 识别 AsrPanel | recording | 所选 ASR 引擎 | 录音 + 识别文本 |
| 工作台 HomePanel | recording | asrEngine | 全链路录音 |
| 工作台 HomePanel | tts | ttsEngine | 回答音频 |
| 对话 ChatPanel | recording | "auto" | **语音提问的录音** |
| 对话 ChatPanel | tts | ttsEngine | **回答的朗读** |
| 实时语音 RealtimePanel | recording | "realtime" | 实时会话录音 |
| 音色管理 VoicePanel | recording | "import" | 导入的克隆样本 |
| 朗读 ReadPanel | tts | 引擎选择 | 朗读文本 |

- `AudioRecord` 目前**没有 source 字段** → 音频库里无法区分一条录音是"识别"还是"对话里的提问"；tts 也分不清是"朗读"还是"对话回答"。默认页签是「我的录音」，所以体感"全是识别来的"。
- 结论：**角标不能只按 kind 二分**，否则对话里的提问也会被标成「识别」。需要新增 `source` 字段（§5.1 方案 B）。

### 2.3 数据现在都存在哪（存储现状盘点）

```
~/Library/Application Support/world.opensound.local/     ← Tauri app_data_dir（标识符 world.opensound.local）
├─ config.json            # 已有：asr-server 路径等启动配置（lib.rs）
├─ audio/
│   ├─ recordings/*.wav   # 已有：录音（16kHz WAV）
│   ├─ tts/*.wav          # 已有：朗读结果
│   └─ index.json         # 已有：元数据索引（kind/engine/text/duration/is_clone_sample）
├─ conversations/          # ❌ 不存在：对话历史目前完全没有
└─ （WebView localStorage "opensound_settings"）  # ⚠ baseUrl/token/deepseekKey/zhipuKey
                                            #    存在 WebView 里而非 app_data_dir，
                                            #    清理 WebView 数据会丢

asr-server 侧（独立进程目录，不在 app_data_dir）：
├─ models/…                            # 各模型文件（见 010）
└─ data/clone-voices/<voiceId>/        # 克隆音色样本+配置（cosyvoice-tts-server.py --voice-dir）
```

### 2.4 对话历史的现状

`ChatPanel.tsx` 的 `messages` 只是内存 state：重启即丢、切面板返回即丢（组件卸载）。语音提问的录音、回答的朗读虽已各自进音频库，但**文字对话本身零留痕**，上下文也无法延续。

## 三、对原 011 方案的评审结论

### 3.1 合理、维持的部分

- **生成策略三方案对比与推荐 B（维持逐句流式 + 单文件归档）成立**。流式低首响是核心优势；A 全文合成对长文不可接受；C 的多文件队列无对应交互需求。「读一句等一句」的正解是进度指示与预读缓冲，不是改架构。
- 4.1 内嵌朗读历史区块、4.3 元数据补齐的方向正确，保留并扩充。

### 3.2 与代码事实不符 / 遗漏的部分（新发现缺口）

| 缺口 | 文档原描述 | 代码实际行为 | 修正方案 |
|---|---|---|---|
| **B′ 停止语义失真** | "stop 后可能存出半截且无标记，也可能不存" | `stop()` 只停本地播放循环；fetch 从未 abort；`collectFrames` 继续读到服务端关流。**点停止后后台仍会把全文合成完，并把完整音频静默入库**——不是半截。既浪费服务端算力，音频库里还会多出一条"你没听完的完整朗读" | speak 时建 `AbortController`；stop 时 `abort()`；`collectFrames` 支持中断时返回已收帧；入库语义见决策点 ③ |
| **D 保存时机绑死播放** | 未提及 | ReadPanel/ChatPanel 都是 `await player.start(...)` 之后才 `.then(collected => saveTts)`。若播完前关 App / 卸载组件，**已合成的音频全部丢失** | 改为"流读完即落盘"：拿到 stream 就挂 `collected.then(save)`，与播放完成解耦 |
| **E 重复入库** | 未提及 | ChatPanel/HomePanel 每次点「朗读」都再存一条同文本 TTS；同一回答反复朗读会堆重复记录 | 决策点 ④：不去重（现状）/ 同文本+引擎去重 / 对话内朗读不入库 |
| **F 元数据仍缺 source** | 只提 voice/sid/speed | 无来源字段 → 角标需求无法精确落地 | `AudioRecord` 增加 `source`（§5.1） |
| **G 朗读 WAV 文件头损坏（历史遗留 bug，P1/P2 验收时发现）** | 未发现 | `mergeWavFrames` 用游标连写头部字符串，RIFF 尺寸字段被覆盖、块名整体前移 4 字节 → **帧流合并的朗读音频从未能被系统解码播放**（录音走另一条正确路径，故只有朗读坏）。已用 afinfo 取证证实：修复前 `AudioFileOpenURL failed`。**后续又发现采样率问题**：三引擎帧均为 24kHz 而合并头写死 16k → 可播但变慢变调（"变成另一个人"）；第一版修复也写死 16k，留下错标文件 | ① 写入函数改为绝对偏移 + **从首帧读真实采样率**；② 时长估算同步改为按头解析；③ Rust `audio_get_dir` 打开面板时两级幂等修复：坏头重建（按 engine 取真实采样率）+ 错标 16k 的 24k 文件改回 24k 并纠正索引时长 |

## 四、生成策略（维持原文结论）

| | A · 全文一次合成为单音频 | B · 维持逐句流式 + 单文件归档（✅ 维持推荐） | C · 逐句多文件 + 队列顺播 |
|---|---|---|---|
| 首声延迟 | =全文合成时间，长文不可接受 | 首句合成时间（现状） | 同 B |
| "读一句等一句" | 换成开场长时间空白 | 可用"句子进度 + 预读缓冲"缓解 | 同 B |
| 历史重听 | 天然单文件 | 单文件（mergeWavFrames 已实现） | 多文件需队列 |
| 中断处理 | 全部作废或半截大文件 | 天然保留已完成句子 | 按句保留 |
| 实现改动量 | 大 | 小 | 大 |

**结论：维持 B。** 本期不做预读缓冲（决策点 ⑧ 可另立项）。

## 五、改造清单（确认后执行）

### 5.1 音频库来源角标（需求 ②）

- **方案 A（轻量·纯前端）**：不加字段，按 kind 显示徽标：recording→「识别」、tts→「朗读」。半天内可见，但对话里的提问录音会被误标成「识别」。
- **方案 B（推荐·source 字段）**：
  - Rust `AudioRecord` + `audio_save` 增加 `source: String`（`#[serde(default)]` 向后兼容）；前端 `saveRecording/saveTts` 加可选参数；
  - 8 个调用点各传自己的来源：`asr` / `home` / `chat` / `realtime` / `voice` / `read`；
  - 徽标文案映射：asr→识别、home→工作台、chat→对话、realtime→实时、voice→音色导入、read→朗读；
  - 旧记录无 source → 回退按 kind 显示「识别 / 朗读」；
  - UI：每条标题前小徽标（复用 `model-cat` 样式，来源不同配色区分），悬停显示完整说明。
- 两方案都顺手在页签名上保持「我的录音 / 朗读历史」不变。

### 5.2 元数据补齐（原缺口 C + F 合并）

- `audio_save` 扩展可选参数：`voice` / `sid` / `speed` / `language` / `interrupted` / `source`；`index.json` 缺省兼容旧记录。
- 历史条目可显示所用音色；为将来"按原参数重读"铺路。

### 5.3 朗读面板内嵌「朗读历史」（原 4.1 保留）

- 位置：朗读面板下半区新区块；`listAudio()` 过滤 `kind==="tts"` 按 `created_at` 倒序取最近 20 条；每次朗读落盘后增量插入顶部。
- 每条：文本摘要（40 字）+ 来源徽标 + 引擎徽标 + 时长 + 时间 + 播放/删除；播放逻辑与音频库共用一个小组件。
- 「在音频库中查看」跳转入口（App.tsx 面板切换机制已有）。
- 入库成功给 toast / 行内提示"已存入朗读历史"，让保存可见。

### 5.4 保存语义修复（缺口 B′ + D）

- speak 开始即创建 `AbortController` 传入 fetch；「停止」= abort 流 + 停播放器；
- 落盘时机改为"流结束（或被中断）即 merge + save"，不再等播放完成；
- 中断时用已收到的帧入库，`interrupted: true` 标记，文本摘要尾部显示「（已截断）」徽标（不污染原文，标记存元数据）；0 帧则不入库仅报错；
- 失败/成功都有可见提示。

### 5.5 对话历史持久化（需求 ③）

- **存储**：`app_data_dir/conversations/index.json`（会话列表元数据）+ `conversations/<session_id>.json`（单会话消息全文）。会话可能很长，故与音频库一致采用"索引 + 分文件"，删除/导出互不影响。
- **Rust 新模块** `conversation_store.rs`，commands：
  - `conversation_list` → index 列表（updated_at 倒序）
  - `conversation_get(id)` → 单会话消息
  - `conversation_save(id, title, messages, meta)` → 整会话覆盖写（每轮对话后由前端调用，异步不阻塞）
  - `conversation_rename(id, title)` / `conversation_delete(id)`
- **消息结构**：`{ role, content, ts, engine?, model?, tts_audio_id? }`。`tts_audio_id` 关联该轮回答朗读在音频库的记录（本期只记录不强展示，为"点击消息重听当轮回答"铺路）。
- **UI（UI 优先，先看得见再谈细节）**：
  - 对话面板顶部工具条：`＋ 新会话`｜`历史 ▾` 下拉（最近会话：标题 / 时间 / 条数，点击载入，带删除）；
  - 当前会话每轮自动保存；标题默认取首条用户消息前 20 字，可重命名；
  - 空态与现有风格一致。
- 自动保存 vs 手动保存、历史上限 → 决策点 ⑤⑥。

### 5.6 数据存储总览与规范（需求 ④）

目标布局（本次实施后）：

```
~/Library/Application Support/world.opensound.local/
├─ config.json            # 启动配置（asr-server 路径…；若采纳决策点⑦，API Key 也迁入此处）
├─ audio/                 # 音频库（已有，本期加 source 等字段）
│   ├─ recordings/*.wav
│   ├─ tts/*.wav
│   └─ index.json
└─ conversations/          # 对话历史（新增）
    ├─ index.json          # 会话列表元数据
    └─ <session_id>.json   # 单会话消息全文

asr-server 侧（独立进程，保持现状，文档备案即可）：
├─ models/…
└─ data/clone-voices/<voiceId>/
```

**六条存储原则**（写入 SPEC 作为后续功能都要遵守的约定）：

1. GUI 用户数据一律集中在 `app_data_dir`，禁止散落在工程目录 / WebView 存储（localStorage 迁移见决策点 ⑦）；
2. JSON 索引 + 资产文件分离，索引只存相对路径；
3. 小索引全量重写即可；大体量内容（会话消息）按 id 分文件；
4. 新增字段必须 `#[serde(default)]`，保证旧 index.json 向后兼容；
5. 删除 = 删文件 + 更新索引；导出统一走 zip（音频+文本）；
6. 设置面板提供「打开数据文件夹」入口（本期一并做，让存储位置用户可见）。

## 六、工作量评估（更新版）

| 项 | 规模 |
|---|---|
| 5.1 source 字段（Rust+8 调用点）+ 角标 UI | 半天 |
| 5.2 元数据扩展（voice/sid/speed/language/interrupted） | 1–2 小时 |
| 5.3 朗读历史区块 + 共用播放小组件 | 半天 |
| 5.4 AbortController + 保存时机 + 截断标记 + 提示 | 半天 |
| 5.5 conversation_store.rs + ChatPanel 历史 UI | 1 天 |
| 5.6 「打开数据文件夹」入口 +（可选）Key 迁移 config.json | 2–4 小时 |
| 回归验证（三引擎 × 长短文 × 中途停止 × 重启恢复） | 半天 |

合计约 3–3.5 个工作日；无服务端协议改动（/speak 保持不变）。

## 七、分阶段执行计划（铁律 1：每阶段完成停下等确认）

- **P1 音频库角标 + 元数据**：source 字段落库 + 八处传参 + 徽标 UI（含旧记录回退）。验收：音频库每条能看到「识别 / 朗读 / 对话…」角标，新录音/新朗读来源标注正确。
- **P2 朗读体验**：朗读面板历史区块 + 保存语义修复（abort / 即时落盘 / 截断标记 / 提示）+ 元数据显示音色。验收：朗读一次面板里立刻出现历史可重听；中途停止得到「已截断」条目；关闭 App 重启仍在。
- **P3 对话历史**：conversation_store + ChatPanel 新会话/历史下拉/自动保存/删除。验收：聊几轮 → 重启 App → 历史下拉能找回并继续。**状态：✅ 编码完成（2026-08-22）**——`src-tauri/src/conversation_store.rs`（list/get/save/rename/delete 五命令，id 校验防路径穿越）、`ui/src/conversationStore.ts`、ChatPanel 顶部「新会话 / 历史▾」工具条 + 每轮自动保存 + 标题自动取首条提问前 20 字 + 该轮朗读音频 id 关联回消息。
- **P4（可选）**：「打开数据文件夹」入口 + API Key 迁移 config.json + SPEC 补存储规范章节。**状态：✅ 编码完成（2026-08-22）**——Rust `PersistedConfig.ui`（base_url/token/deepseek_key/zhipu_key，只覆盖传入字段）+ `get/set_ui_settings` + `get_data_dir/open_data_dir`；前端启动时 `initSettings()` 载入缓存并从 localStorage 一次性迁移旧值后清除；设置面板全部字段 600ms 防抖自动保存 + toast「已保存」，新增「📂 打开数据文件夹」与数据目录展示；`SPEC.md §六` 补存储规范。

## 八、决策结果（已拍板，2026-08-22）

| # | 问题 | 决定 |
|---|---|---|
| ① | 生成策略 | **维持流式 B** |
| ② | 角标粒度 | **方案 B：source 六类来源角标**（识别/工作台/对话/实时/音色导入/朗读），旧记录按 kind 回退显示 |
| ③ | 手动停止后的已收帧 | **入库 + 标记「已截断」** |
| ④ | 重复朗读入库 | **每次都存，不去重**（保持现状） |
| ⑤ | 对话保存方式 | **每轮自动保存** |
| ⑥ | 会话数量上限 | **不设限，手动删** |
| ⑦ | API Key 迁移 | **迁入 config.json**；且设置面板填写后**自动保存 + toast 提示「已保存」**（归 P4 执行） |
| ⑧ | 按原参数重读 / 预读缓冲 | 本期都不立项。含义说明：重读=历史条目上"用原音色再生成一遍"按钮；预读缓冲=播放第 N 句时后台提前合成第 N+1 句。**补充硬性约定：任何重读/再次生成的音频一律作为独立新条目分开保存，不覆盖、不混淆原条目**（与 ④ 一致） |

执行状态：**P1、P2 已获批开工**；P3、P4 待前两阶段验收通过后再动。
