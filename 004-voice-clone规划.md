# 004 · 语音克隆规划（录音 → 克隆音色 → 朗读）

> 状态：**模型下载中**（`005-cosyvoice模型下载.md` 命令执行中）；**克隆后端 `/clone` 与 GUI 均未实现**。
> 📌 **实施任务已移交 `006-voice-后续规划.md`（最新参考规划）**。
> 定位：把「用户录音」变成可复用的「克隆音色」，供朗读/对话引擎选用。
> 衔接：`000-voice-tauri-app规划.md §1.4`（功能愿景）、`002-voice-research.md §三`（克隆模型调研）、`003-audio-storage规划.md`（音频库：录音落盘 + 克隆样本标记已实现）。
> 本文为**专项实施规划**，记录已定决策、现状与实施步骤，供后续接手直接执行。

---

## 〇、已定决策（勿再讨论）

| 项 | 结论 |
|---|---|
| 克隆引擎 | **CosyVoice 3.0**（~4GB，Apache2，3s 零样本克隆，中英日韩粤，质量/内存平衡） |
| 设备 | Apple **M4 / 16GB** 统一内存 —— 可跑（需 ~4GB），性能可接受 |
| 备选 | ZipVoice（sherpa-onnx 同栈，纯 CPU 轻量，接入最快，克隆质量中等）——仅当想快速跑通时 |
| 明确不做 | DOTS-TTS（2B 自回归，16GB 上偏吃力，不首推） |

---

## 一、现状（已完成，复用）

1. **录音落盘**（003）：用户录音 → `app_data_dir()/audio/recordings/*.wav`（16kHz 单声道）+ 元数据 `index.json`。
2. **克隆样本标记**（003 P3）：录音可在「音频库」标记「🎨 作样本」，`is_clone_sample=true` 存 index.json，作为克隆参考音频来源。
3. **TTS 播放链路**（003）：asset protocol 播放已打通；朗读/对话引擎可选 kokoro/qwen3。

> 需要新做的：把标记的录音样本 → 喂给 CosyVoice 3.0 → 生成克隆音色 → 保存 → 朗读引擎可选用。

---

## 二、实施步骤（建议顺序）

### 阶段 A · 克隆后端（asr-server 接入 CosyVoice）
- 新增 Python 推理环境（复用现有 `asr-server/qwen3-tts-server.py` 的集成模式：独立子进程 + HTTP 端口）。
- 模型下载：CosyVoice 3.0（~4GB，走 hf-mirror）。
- 新增端点（参照现有 `/speak` / `/voice-chat` 风格，并入 9528 主服务）：
  - `POST /clone`：传入参考录音 + 提示文本 → 生成/更新克隆音色。
  - 克隆音色保存：`app_data_dir()/audio/clone-voices/<voice_id>/`（参考音频 + 生成配置）。
  - `/speak` 扩展：`voice=克隆音色 id` 时走 CosyVoice 克隆合成。
- 能力上报：`/health` 增加克隆引擎就绪状态。

### 阶段 B · GUI（UI 优先）
- 「模型管理」或新增「音色管理」：列出克隆音色、新建（从音频库选已标记样本）、删除、改名。
- 朗读/对话面板的引擎下拉可加「克隆音色」选项。
- 新建克隆音色的交互：选录音样本 → 输入/自动一段参考提示 → 点「生成」→ 进度提示 → 音色就绪。

### 阶段 C · 打磨
- 克隆音色与录音样本解耦（可删原始录音但保留音色）。
- 多个克隆音色并存；音色试听。

---

## 三、风险 / 注意

- CosyVoice 需 Python/PyTorch 环境，与现有 Node(sherpa) 栈不同 —— 独立进程承载，避免污染主服务。
- 模型下载 ~4GB，需预留磁盘；首版可"按需首次下载"（复用 `/install-model` NDJSON 进度）。
- 克隆质量依赖参考录音：样本建议 3–10s、清晰单人声。

---

## 四、待新对话确认

1. 是否按「阶段 A 后端先行、阶段 B GUI 跟上」推进？
2. 首版只做「单个克隆音色」还是「多个音色管理」？
3. 是否同时准备 ZipVoice 作为轻量回退（同 sherpa 栈可快速验证）？
