# 000-plan-12 · OpenSound 官网规划（设备能力 + 云端能力 + 浏览器端侧模型）

> 定位：规划 OpenSound 官网——它不只是一张宣传页，而是一个**轻量可用的网页版语音工作台**：功能上 = **设备能力（浏览器/系统朗读识别）+ 云端能力（识别/朗读/LLM）**，比 App 少的是**本地部署大模型能力**（由浏览器端侧小模型补位一部分）；App 已规划未实现的能力（音频编辑、音频素材库等）同样进官网。同时承担官网本职：介绍、下载、文档。
> 依据：zitan `017`（浏览器版 SenseVoice 方案）/`018`（三大浏览器原生语音识别 API）/`023`（平台×浏览器识别能力矩阵）/`028`（各平台内置朗读全景）；本项目 `000-plan-11`（云端通道）、`000-plan-7`（backhill R2 音频素材 API）、`000-plan-8`（音频编辑）、`000-plan-9`（人声伴奏分离）、`007`（模型）、000-summary §二（开放后端 9528）。
> 立场：**zitan 的浏览器端实践是这个官网的直接前身**——引擎矩阵、浏览器识别踩坑、云端配置区都可直接复用/移植。

---

## 一、产品定位与功能矩阵

### 1.1 一句话定位

> App = 全能力（本地大模型 + 本地小模型 + 系统 + 云端）；官网 = 设备能力 + 云端能力 + 端侧小模型；两者共享同一套云端供应商与交互语言。

### 1.2 功能矩阵（App vs 官网）

| 能力 | App | 官网 | 官网实现方式 |
|---|---|---|---|
| 识别 ASR | 本地 SenseVoice/Whisper/原始版 + 系统识别 + 云端（plan-11 A-1） | ✅ 设备 + 云端 + 端侧小模型 | Web Speech API（Safari/Edge 国内可用，见 §二.1）；云端 zhipu-asr（Key 或代理）；端侧 sherpa-onnx WASM SenseVoice（§二.2） |
| 朗读 TTS | 本地三引擎 + 系统音色 + 云端（plan-11 T-1/T-2） | ✅ 设备 + 云端 + 端侧小模型 | `speechSynthesis` 系统音色（全平台免费离线，zitan 028）；云端 glm-tts/OpenAI 兼容；端侧 kokoro-js（§二.2） |
| 对话 LLM | 本地 llama-cpp/Ollama + 云端 DeepSeek/智谱 | ✅ 云端 + 端侧小模型 | 云端直连/代理；端侧 WebLLM（WebGPU 跑 Qwen3-0.6B 级别，§二.2） |
| 语音克隆 | CosyVoice3 本地 + 云端克隆（plan-11 C-1） | 🔶 仅云端克隆 | glm-tts-clone / fish.audio 克隆接口 |
| 实时语音 | 本地 cpal+VAD 链路 | 🔶 降级版 | 麦克风 MediaRecorder + 云端 ASR/TTS 串联（非实时流式） |
| 环境音素材库 | 规划中（000-plan-7） | ✅ 直接做 | 复用 backhill R2 API（`whitenoise.earthtrip.online`，目录 JSON + Howler 多轨混音，zitan/backhill 现成消费端代码） |
| 音频编辑 | 规划中（000-plan-8） | ✅ 轻量版 | 剪裁/拼接/音量/淡入淡出（浏览器 WebAudio 本地即可，无需后端） |
| 人声伴奏分离 | 规划中（000-plan-9） | ⬜ 暂不做 | 模型太重（浏览器跑不动），入口留给 App 下载引导 |
| 模型管理 | ✅（下载/换装/卸载） | ❌ 不做 | 端侧小模型按需自动下载缓存（IndexedDB/Cache API），无管理页 |
| 开放后端 | ✅ 9528 | — | 官网不开放后端；若用户本机开着 App，官网可探测 `127.0.0.1:9528/health` 提供「连接我的 OpenSound」增强模式（可选彩蛋，见 §四.4） |

---

## 二、浏览器端侧小模型调研（补"没有本地部署"的空档）

> 结论先行：**端侧不是噱头，是官网免费/离线档的真实支撑**。效果最好的组合都已由 zitan 017 实测或官方维护。

### 2.1 设备能力层（零下载，优先用）

| 能力 | API | 后端 | 可用性（zitan 018/023 已核实） |
|---|---|---|---|
| 识别 | `SpeechRecognition` / `webkitSpeechRecognition` | Chrome=Google 云（国内被墙❌）/ Edge=Azure（国内可用✅）/ Safari=SFSpeechRecognizer（装语言包可离线，国内✅） | 特性检测构造函数存在即用；引擎矩阵沿用 zitan `EnvDetect.available()` 规则 |
| 朗读 | `speechSynthesis` | 各 OS 系统音色（mac 增强音色质量可接受） | 全平台可用、免费离线；`getVoices()` 列表 + 语言过滤（zitan 028） |

### 2.2 端侧小模型层（下载一次缓存后离线可用）

| 任务 | 推荐 | 体量 | 效果与依据 |
|---|---|---|---|
| ASR | **sherpa-onnx WASM + SenseVoice int8** | 239MB | zitan 017 结论：官方维护的浏览器方案（wasm/vad-asr 演示已跑通），WASM 推理 100–500ms，中英日韩粤+情感；transformers.js 不支持 SenseVoice（2026 核实），onnxruntime-web 自建 DSP 不推荐 |
| ASR（轻量备选） | transformers.js + **Whisper base/base-zh q8** 或 **Moonshine tiny/base** | 75–115MB | Whisper 多语兜底（本项目 App 已用同款）；Moonshine 27M 参数、短音频低延迟，transformers.js 官方支持 |
| ASR 前置 | Silero VAD（sherpa-onnx 自带） | ~2MB | 切句省流量省电 |
| TTS | **kokoro-js（Kokoro-82M int8）** | ~80MB | HuggingFace 官方浏览器包，WebGPU/WASM 可跑，中文音色可用；质量为端侧 TTS 第一梯队 |
| LLM | **WebLLM（MLC，WebGPU）+ Qwen3-0.6B/1.7B q4** | 0.5–1.5GB | 端侧 LLM 唯一成熟路线；仅限支持 WebGPU 的桌面浏览器，移动端不做 |
| 声音克隆 | 端侧不做 | — | 无成熟浏览器方案，留给云端（plan-11 C-1） |

> 端侧模型分发：走 hf-mirror + 官方源双源（复用本项目 000-summary §十 换源经验）；缓存在 Cache API/IndexedDB，站点提供「清除端侧模型缓存」入口。

### 2.3 引擎选择策略（官网工作台）

```
识别：设备 Web Speech API（可用且用户允许） → 端侧 SenseVoice（离线/大段文本） → 云端（填了 Key 且要求高精度/方言）
朗读：设备 speechSynthesis（默认秒开） → 端侧 kokoro-js（要更自然） → 云端（填了 Key）
对话：云端（默认，填 Key） → 端侧 WebLLM（显式开启，桌面 WebGPU）
```
每档引擎在 UI 上标注「设备 / 端侧(需下载) / 云端(出网)」，与 App 的隐私口径一致。

---

## 三、官网信息架构（站点结构）

```
/                首页：品牌 + 能力演示（每能力一个可立即点击试用的卡位）+ 下载 App 入口
/tools/recognize  识别工作台（录音/上传 → 设备|端侧|云端 出文本，复制/导出）
/tools/read       朗读工作台（文本 → 设备音色|端侧|云端 出音频，可下载）
/tools/chat       对话工作台（文字/语音 → 云端 LLM → 可朗读回答）
/tools/ambient    环境音（backhill R2 多轨混音，plan-7 的网页形态）
/tools/edit       音频编辑轻量版（WebAudio 剪裁/拼接/音量/淡入淡出，plan-8 子集）
/download         App 下载 + 三平台说明 + 与网页版能力对照表（本表 §1.2）
/docs             文档：快速上手 / 云端 Key 配置指南（智谱/DeepSeek/fish.audio）/ 隐私说明（哪档出网）
/about            关于与开源仓库
```

- **视觉与交互语言**：直接沿用 App 的面板结构（Panel/引擎徽标/引擎下拉），让「网页版 = App 的零安装预览」心智成立；zitan 的引擎可见性矩阵/浏览器检测代码直接移植。
- **深浅色 + 移动端适配**：tools 各页一屏一事，手机上可用（录音走 MediaRecorder，iOS Safari 需用户手势触发——zitan 003/004 已有踩坑结论）。

## 四、云端能力接入架构（官网侧）

### 4.1 两种形态，按阶段演进

1. **阶段 1（零后端）：用户自填 Key 直连**
   - 纯静态站（Cloudflare Pages，zitan 006 已有完整部署方案）；
   - Key 存 localStorage（zitan 现有做法），浏览器直连智谱/OpenAI 兼容/Azure；
   - ⚠️ 需逐家核实 CORS：智谱/OpenAI 兼容层多允许跨域；Azure 允许；不支持的就进代理名单。
2. **阶段 2（轻后端代理）：免费档 + 托管 Key**
   - Cloudflare Workers 轻代理（隐藏 Key、限流、统计用量），支持 `s2.1-pro-free` 这类免费档让访客零 Key 试用；
   - 代理只转发 plan-11 的三个云端端点（asr/tts/chat），不做存储。
3. **可选彩蛋：连接本机 App**——探测 `127.0.0.1:9528/health`（App 已是开放后端），可用时把云端档换成「我的 OpenSound 本地服务」档（本地大模型能力经此回归网页版）。

### 4.2 Key 与计费口径

- 用户 Key：额度/计费归用户自己的供应商账号，官网不碰钱；
- 托管 Key（阶段 2）：只覆盖免费档展示用量，超限引导自填 Key。

## 五、与既有资产的关系（不重复造轮子）

| 资产 | 复用方式 |
|---|---|
| zitan（网页版语音工作台） | 官网 tools 的直接前身：浏览器识别实现/踩坑（003/004/017/018/023）、云端配置区交互（030）、多场景文本机制（032）、部署方案（006）整体移植 |
| backhill R2 素材 API | 环境音模块分发侧零新建（000-plan-7 已实查 schema 与协议注意） |
| OpenSound asr-server 帧流协议 | 若走「连接本机 App」彩蛋，前端帧流播放器（createFramePlayer）与 UI 代码可复用 |
| plan-11 云端通道 | 官网云端档 = 同一套供应商与端点，接口层保持双形态（直连/代理） |

## 六、实施阶段（每阶段真人验收）

| 阶段 | 内容 | 验收（真人） |
|---|---|---|
| 0 · 立项定案 | 确认官网形态（纯静态 → 轻代理）、域名、视觉基调 | 本文档评审通过 |
| 1 · 官网骨架 + 设备能力 | 首页/下载/文档 + 识别/朗读两个工作台（Web Speech + speechSynthesis，引擎矩阵移植 zitan） | 手机+电脑双端：Safari/Edge 出声出字；Chrome 国内如实显示不可用 |
| 2 · 云端档 | 三工作台接云端（自填 Key），隐私标注 + 未填 Key 拦截 | 填智谱 Key 后三能力全通；断网/错 Key 报错清晰 |
| 3 · 端侧小模型 | SenseVoice WASM + kokoro-js 按需下载缓存，双源换源 | 断网后端侧识别/朗读仍可用；清缓存可释放 |
| 4 · 素材库 + 音频编辑 | 环境音混音页（backhill R2）+ WebAudio 轻编辑 | 多轨混音自然；剪裁导出可用 |
| 5 · 轻代理 + 免费档 | Workers 代理 + s2.1-pro-free 零 Key 试用 + 「连接我的 OpenSound」彩蛋 | 无 Key 访客可体验云端朗读/识别；开着 App 的用户官网可连本地服务 |

## 七、风险与待核实清单（043 纪律：先核实再动手）

- [ ] 智谱/OpenAI 兼容/Azure 端点的 CORS 逐家实测（决定直连 vs 代理名单）；
- [ ] Web Speech API 在目标部署区（大陆/非大陆）的可达性按 023 矩阵真机复核一次（iOS 第三方浏览器行为未实测项）；
- [ ] kokoro-js 中文音色质量真机试听（决定是否值得 80MB 下载）；
- [ ] WebLLM WebGPU 覆盖面（Safari 26 / Chrome 桌面）实测；
- [ ] backhill R2 桶 CORS 与音源协议逐条复核（plan-7 §1.3 已列红线）；
- [ ] macOS Safari 的 speechSynthesis 增强音色在网页端可见性（部分增强音色 WebView 拿不到，需实测）。

## 变更记录
| 日期 | 变更 |
|---|---|
| 2026-09-06 | 初版：官网定位（设备+云端+端侧小模型，App 全能力对照矩阵）；端侧小模型选型（SenseVoice WASM / kokoro-js / WebLLM，基于 zitan 017/018/023 既有调研）；信息架构、云端双形态接入（静态直连→轻代理）、zitan/backhill 资产复用清单、六阶段实施与风险核实清单 |
