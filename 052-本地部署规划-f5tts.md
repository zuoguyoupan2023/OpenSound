# 052 · 本地部署规划：F5-TTS（SWivid，MIT，零样本克隆）

> 目的：F5-TTS **独立本地验证/部署**（先源/路径/出声，最后回 App）。纪律见 043 §一；总清单见 043 §〇（新增候选）。
> 关联：流程与踩坑口径复用 **047**。

---

## 一、这是什么 / 效果依据

| 项 | 内容 |
|---|---|
| 官方仓库 | `SWivid/F5-TTS`，MIT 许可；0.3B（F5-base）与 1.2B（F5-1.5）两档 |
| 特点 | 零样本克隆效果好（榜单中文/英文 WER 优秀），DiT + flow matching 架构 |
| 显存 | 0.3B ~2-3GB fp16；1.2B ~6-8GB fp16（12GB 可行但吃紧） |
| 已知坑 | **推理 RTF 慢、无流式**（[Issue #81 讨论](https://github.com/SWivid/F5-TTS/issues/81)）；长文本需拆句 |

## 二、独立部署步骤（顺序执行，每步停下汇报）

1. **源实测**：README raw（安装命令/`f5-tts` pip 包）；requirements 登记（torch 版本）；HF 权重（`SWivid/F5-TTS`）整仓字节清单。
2. **独立环境**：目录（如 `E:\Github\f5tts\`）；venv + CUDA torch cu128（aliyun 平铺镜像）；PyPI aliyun 镜像；**预装 soundfile**（047 坑4）。
3. **模型**：整仓（0.3B 优先）+ 字节校验；vocoder/辅助件预下载（036 坑 F）。
4. **验证**：import 探针 + 克隆出声（参考音频 + 文本）；测 RTF（0.3B vs 1.2B）。
5. **验收**：出声 wav + torch/cuda、体积、RTF、克隆效果、许可登记。
6. **坑映射（047 全套）**：urllib（坑1）；磁盘余量（坑2）；flash_attn grep（坑3）；soundfile（坑4）；分进程（坑5）；签名 bug（坑6）；UTF-8（坑7）。

## 三、风险/待实测登记
- ⚠️ RTF 慢（无流式，DiT 迭代）是最大体验风险；长文本拆句策略；vocoder（vocos）依赖版本。

## 变更记录
| 日期 | 变更 |
|---|---|
| 2026-08-30 | 初版：F5-TTS 独立部署规划（来源：用户提出 + 榜单调研） |